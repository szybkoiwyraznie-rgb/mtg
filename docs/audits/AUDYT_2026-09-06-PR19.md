# Audyt startowy — PR-19 (audyt PR-18 + naprawy Z1–Z5 + rewamp mapy Alary v2) — 2026-09-06

Sesja `arena/01a076ea-mtg`, punkt wyjścia = scalony PR-19 (`2a26531` = squash
sesji PR-19 na `main`, 2026-09-06 15:30). Audyt wg AGENTS.md §5: weryfikacja
poprawności i logiki zmian (treści i kodu), nie raport z testów.

## Integralność (punkt wyjścia)

- `npm test`: **104/104 zielone** (zgodnie z audytem PR-18 i handoffem PR-18).
- `npm run build`: zielony — 10 stron (5 kart, 0 haseł, 5 planów); drzewo map
  w `dist/maps/`; ZIP = 11 plików.
- `python3 tools/map-audit.py`: **RAZEM PROBLEMÓW: 0** (wszystkie mapy
  łącznie z warsztatem i podmapą Midgaru).
- Klon pogłębiony (`git fetch --unshallow`) — pełna historia dostępna dla
  stopek czasu (ADR 0029).

## Zakres PR-19 (diff `4bc1234..2a26531`, 14 plików)

Trzy commity (wg `gh pr view 19`):
1. `fe5675f1` — audyt PR-18: wynik + kolejka napraw (Z1–Z5).
2. `deb7cc51` — **naprawy Z1–Z5 z audytu PR-18**.
3. `b1904dd8` — **Alara — rewamp mapy po recenzji właściciela**.

Uwaga procesowa: opis PR-19 na GitHubie wymienia tylko commit 1 (audyt +
„kolejka czeka na zlecenie"), podczas gdy gałąź niosła też wykonane naprawy
i rewamp mapy. Opis nie był aktualizowany kumulatywnie (AGENTS.md §7 pkt 4)
— znalezisko procesowe Z-P1 niżej.

## Ocena — zgodne z ADR (bez zastrzeżeń)

### Naprawy Z1–Z5 (commit `deb7cc51`) — poprawne i kompletne

1. **Z1 („Fabuła dostawy" → „Fabuła"):** w kartach 275FIN i 305ARB termin
   zastąpiony przez „Fabuła właściciela"/„Fabuła" (doprecyzowanie
   ADR 0026 z 2026-09-05); w sekcjach Źródła obu kart dodane wpisy
   „Fabuła właściciela (2026-09-05) — …" z opisem kotwicy. Zgodne
   z brzmieniem i rolą (kotwica osadzenia, nie kanon MtG).
2. **Z2 (odsyłacze ADR w treści kart):** usunięte z 275FIN/305ARB
   („(ADR 0026)", „(ADR 0033…)"); zdania przepisane tak, że sens został
   (np. „jednej mapie scalnego planu" zamiast „(ADR 0033: jedna mapa…)").
   Test ui-smoke (niżej) potwierdza brak „ADR" w treści wszystkich kart.
3. **Z3 (luka pokrycia ui-smoke):** pętla po wszystkich plikach
   `content/cards/*.md` z asercjami zakazu „Fabuła dostawy"/„ADR"/„verbatim"
   (linie ~332–350 `test/ui-smoke.test.js`) — usuwa lukę systemowo,
   kolejna materializacja jest pilnowana od pierwszego commita.
4. **Z4 (source snapshotów):** 275FIN → `cards/named?exact=Aerith+Rescue
   Mission`, 305ARB → `cards/named?exact=Illusory+Demon&set=arb` — zgodnie
   z ADR 0004 pkt 2; pola `pobrano`/`slug`/`notka_numery` nietknięte.
5. **Z5 (plany cytujące ADR):** usunięte odsyłacze z `content/planes/alara.md`
   i `content/planes/final-fantasy.md` (sekcje Źródła też); styl stron planów
   ujednolicony ze starszymi planami (srodziemie/zendikar/ravnica nie cytują
   dokumentów repo).

### Rewamp mapy Alary v2 (commit `b1904dd8`) — zgodny z ADR i kanonem

1. **Topologia:** Esper jako **archipelag wysp na Morzu Esper** (z akwenami
   Dwindling Sea, Sea of Stars, Inkwell — notka map.json: „morze wewnętrzne/
   wschodnie/południowe Esper" wg referencji fanowskiej); Maelstrom jako
   **równoprawny węzeł-shard** w punkcie złączenia (etykieta „The
   Maelstrom", pseudo-biom `wir`); fraktalne linie brzegowe „w stylu
   Zendikaru". Zgodne z ADR 0033 (jedna mapa aktualnego stanu kanonicznego,
   post-Conflux) i ADR 0031 (fanowska referencja jako baza topologii,
   raster poza gitem).
2. **Silnik mapforge:** nowy klocek/pseudo-biom `wir` (koncentryczne
   przerywane pierścienie + spirala Archimedesa) w `tools/mapforge/bloki.mjs`
   i rejestr `BLOKI_BIOMOW` w `render.mjs`. Kod czysty, bez zależności
   (ADR 0002), deterministyczny (rng z hasha id), achromatyczny (`PAL.tekst`
   — motyw atlas, ADR 0019/0021). Spójny z zasadą „nowy typ obiektu =
   klocek w warsztacie, nie ręczny `<path>`" (ADR 0018/0028).
3. **Dane:** `scena.json` — 14 lądów, 12 biomów, 5 pasm, 4 rzeki, 11 dróg,
   28 POI, 40 etykiet, 3 strefy wodne (Dwindling Sea/Sea of Stars/Inkwell)
   + 3 etykiety wodne; `map.json` — 40 kotwic z `pozycja_zrodlo` per pozycja
   (kanon mtg.wiki albo „topologia wg referencji fanowskiej" — rozróżnienie
   uczciwe, ADR 0010/0013). Pinezka 305ARB (`0.475, 0.475`, pewność
   `dokladna`) — zweryfikowano point-in-polygon: **leży wewnątrz poligonu
   wiru Maelstromu** (środek wiru ~950/665 px przy 2000×1400). Zgodne
   z Fabułą (scena w Maelstromie) i ADR 0026.
4. **Nazwy:** wyłącznie kanoniczne (MTG Wiki, Category:Alara locations) —
   Topa, Valeron, Akrasa, Jhess, Eos, Sun-Dappled Court, Serul Cove,
   Fort Dawnray, obeliski Bantu, Cathedral of Bliss, Sacellum, ruiny
   Antali, Qasal, Etlan, Summoning Root, Mount Jhal, The Boiling Slopes,
   The Sweltering Cauldron, Hellkite's Pass, Sedraxis, Unx, Torchlight,
   Boneheaps, Kederekt, Sanctum Arcanum, Vectis, Palandius, Tidehollow.
   Bez fanowskich wynalazków (zasada z ADR 0032 pkt 5 — przeniesiona na
   Alarę).
5. **QA:** wszystkie podkłady repo parsują się jako XML; map-audit 0
   problemów; testy stylu (whitelista kolorów) i wiązań zielone.

## Znaleziska (kolejka napraw tej sesji)

**A1 [TREŚĆ, średni] Strona planu alara nie odzwierciedla mapy v2.**
`content/planes/alara.md` (sekcje lead/„Setting w pigułce"/„Mapa") opisuje
stan sprzed rewampu: „Esper z morzami wewnętrznymi na wschodzie", Maelstrom
jako „plama w punkcie złączenia shardów", a lista POI (Topa, Valeron…,
Sanctum Arcanum, Sedraxis) to podzbiór v1. Mapa v2: Esper = **archipelag**
na Morzu Esper z akwenami Dwindling Sea/Sea of Stars/Inkwell; Maelstrom =
równoprawny węzeł (etykieta „The Maelstrom"); **28 POI** (m.in. Vectis,
Palandius, Tidehollow, Serul Cove, Mount Jhal, The Boiling Slopes,
Sedraxis, Unx, Kederekt). Treść planu ma opisywać mapę, którą faktycznie
renderuje (ADR 0013: proweniencja i stan w danych + tekście strony).

**A2 [PROCES] Brak wpisu w `content/co-nowego.md` o zmianach PR-19.**
Ostatni wpis dziennika: 2026-09-05 22:15 (mapa Alary v1). Rewamp v2
(2026-09-06) i naprawy Z1–Z5 scalono 2026-09-06 15:30 — wymagany wpis
wg ADR 0029 (nagłówek `## 2026-09-06 15:30 — …`).

**A3 [PROCES] Brak wpisu o sesji PR-19 w `docs/PROJECT_HISTORY.md`**
(dziennik kończy się na sesji PR-18) **i brak handoffu sesji PR-19**
(najnowszy `docs/setup/HANDOFF_*.md` = PR-18). Sesja PR-19 nie domknęła
dokumentacji wg AGENTS.md §7.

**A4 [NISKI] `docs/ROADMAP.md` opisuje PR-19 jako „w toku"** — PR-19
scalony; wpis do aktualizacji (skrót + odesłanie do ADR 0032/0033).

**Z-P1 [PROCES, niski] Opis PR-19 nieaktualizowany kumulatywnie** — na
GitHubie opisuje tylko audyt; wykonane naprawy i rewamp mapy nie są
w nim udokumentowane. Lekcja dla tej sesji: aktualizować opis PR po
każdym commicie merytorycznym (AGENTS.md §7 pkt 4).

## Wnioski

PR-19 bez wad blokujących: integralność w pełni zielona, naprawy Z1–Z5
wykonane poprawnie i systemowo (pętla ui-smoke), rewamp mapy Alary v2
zgodny z ADR 0031/0033 i kanonem nazw, pinezka 305ARB w Maelstromie
(zweryfikowane geometrycznie). Główny problem to **rozjazd dokumentacji
ze stanem repo**: strona planu alara opisuje mapę v1 (A1), a co-nowego /
PROJECT_HISTORY / handoff / opis PR nie obejmują scalonych zmian (A2–A4,
Z-P1). Kolejka napraw A1–A4 to pierwszy blok pracy bieżącej sesji —
roadmapa: `docs/plans/PLAN_2026-09-06-pr20-audyt-pr19-i-kolejka-napraw.md`.
