# Audyt — stan po poprzednim PR (punkt startowy sesji PR-3)

- **Audytowany stan:** `main` @ `92b994c` (squash „PR-1: Fundamenty", ale w
  rzeczywistości zawiera także domkniętą materializację PR-2 — 2 karty,
  2 plany, 2 mapy; historia gita jest spłaszczona do jednego commita).
- **Sesja audytująca:** 2026-08-31 (PR-3)
- **Podstawa:** AGENTS.md §5; zakres = przegląd stanu projektu PO poprzednim
  scaleniu wobec założeń ADR-ów.

## Integralność (AGENTS.md §5.1)

| Sprawdzenie | Wynik |
|---|---|
| `npm test` | 65/65 zielone |
| `npm run build` | OK — 4 strony (2 karty, 2 plany, 0 haseł), 14 modułów, 2447.4 kB |
| Artefakt jednoplikowy (`file://` + Pages) | tak (ADR 0001) |

## Merytoryczność treści (AGENTS.md §5.2)

- **Karta 1LTR Dunland Crebain** — kompletne 10 sekcji szkieletu; 5 źródeł
  z URL; flavor: posiadany wydruk **nie ma** flavoru, sekcja mówi to wprost
  (bez cytowania flavoru innego printu — zgodnie z kontraktem).
  Zgodna z chudym formatem (ADR 0011): brak sekcji narracji/promptu,
  brak „Wątków i Powiązań" (encje pogrubione, wikilink dopiero po progu
  2 kart). Hierarchia kanonu (ADR 0010/0011): byty z karty + lore świata.
- **Karta 2BFZ Coralhelm Guide** — kompletne 10 sekcji; 5 poz. źródeł
  (w tym 2 oznaczone „wiedza ogólna" bez URL-a — dopuszczalne, jawnie).
  Flavor Jori En z tłumaczeniem; transpozycja jawnie „natywna".
  Zgodna z chudym formatem.
- Oba pliki `content/cards/*.md` bez meta-tekstu procesowego (ADR / docs/ /
  SZKIELET / „Status:" / „verbatim") — czysta treść encyklopedyczna.
- **Plany** (Śródziemie, Zendikar): rejestrowe, funkcjonalne, `typIP`
  poprawne, odnośnik do mapy. Krótkie — najsłabsza warstwa treści
  (przekazuje się do pogłębiania w Pętli Jakości).

## Poprawność danych (AGENTS.md §5.3)

- **Snapshots Scryfall:** oba kompletne (name, mana_cost, type_line,
  oracle_text, set, rarity, artist, image_uris.normal, + metadata
  `source`/`pobrano`/`slug`). 1LTR odpowiada wydrukowi **borderless #411,
  David Rapoza** — spójne z „Drukiem w Kolekcji". 2BFZ = #74, Viktor Titov.
- **Slugi/tagi:** slugi wg wzorca; tagi wszystkich stron w słowniku
  `content/taxonomia.json` (fauna, szpiedzy, wojna; ekspedycje, geografia).
- **Pinezki:** obie w `map.json` z `pewnosc: region`, z `uzasadnienie`
  z lore; frontmatter `pinezka.mapa` = plan. Spójność test `test/mapy.test.js`.
- **Mapy:** `wariant` (T2 Śródziemie / T3 Zendikar), `zrodlo` z URL,
  licencją i datą pobrania; kotwice (Śródziemie — z XML podkładu;
  Zendikar — ręczne); `regiony: []`; `rekonstrukcja` flagi.
- **Rejestr ADR:** statusy i tabela spójne (`test/rejestr-adr.test.js`).

## Wnioski

1. Baza jest w stanie zielonym i spójnym z ADR-ami; treść kart w pełni
   kanoniczna (ADR 0011), bez dublowania wiedzy.
2. **K5 otwarte:** brak `tools/wiki-stats.mjs` (completeness score) —
   Pętla Jakości nie jest jeszcze wymierna; to główny kamień do domknięcia.
3. **Hasła:** 0 — poprawnie. Żadna encja nie jest jeszcze wspominana przez
   ≥2 karty (karty z różnych planów).
4. **Stalarność handoffu:** `docs/setup/HANDOFF_2026-08-31.md` opisuje stan
   „PR-2 w toku / PR-1 przed domknięciem workflow'ów" — nie odpowiada
   aktualnemu stanowi (workflow-y dodane, materializacja domknięta).
   Zastąpić go nowym handoffem w tej sesji (dokument jednorazowy).

**Rekomendacja dla tej sesji:** domknąć K5 (wiki-stats) + pogłębić strony
planów + zaktualizować dokumenty jednorazowe (handoff, co-nowego, historia).
