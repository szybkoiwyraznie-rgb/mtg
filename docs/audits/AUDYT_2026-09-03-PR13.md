# AUDYT 2026-09-03 — PR #13 (scalony jako `72d6bac`)

> Audyt poprzedniego scalonego PR przed nową pracą (AGENTS.md §1.2/§5).
> Metoda: pełny diff PR (#13, squash-merge 2026-09-03 09:12 UTC, 43 pliki)
> czytany jako recenzja (treść + kod), weryfikacja integralności
> (`npm test` 102/102, `npm run build` OK — artefakt 303.2 kB, ZIP
> 12733.8 kB — zgodne z handoffem), `map-audit.py` ravnica|zendikar|
> srodziemie = 0.

## Zakres (3 warstwy sesji w jednym squash-PR-ze)

1. **Pętla Jakości 2026-09-02** (audyt PR-12 + pogłębienie geografii
   Zendikaru: sekcje Bala Ged i Sejiri + cytowania; `HANDOFF_2026-09-02-pr13`).
2. **Dostawa 137GPT Withstand + plan Ravnica + mapa v1** (T4, klocki
   miejskie mapforge; `HANDOFF_2026-09-03-pr13`).
3. **Mapa v2 (kalibracja pod transkrypcję GGR)** + **LORE-first (ADR 0030)
   + ADR 0031** (`HANDOFF_2026-09-03-pr13-mapa-v2`,
   `HANDOFF_2026-09-03-pr13-lore-first`).

## Treść — zgodna z ADR-ami

- **Wpis dostawy** (`collection/entries/137gpt-withstand.md`): format v3
  (ADR 0026: imgId · nazwa · set · plan · Fabuła), verbatim, frontmatter
  normalizowany; archiwum nie ruszone w innych miejscach. ✓
- **Snapshot Scryfall** (`scryfall/137gpt-withstand.json`): pełny (nazwa,
  koszt `{2}{W}`, typ, Oracle, set GPT, rzadkość, artysta, flavor,
  `image_uris` linkowane) + metadane `source`/`pobrano`/`slug` (ADR 0004). ✓
- **Karta 137gpt**: 9 sekcji w kolejności ADR 0030 (Kronika Lore →
  Postacie i Byty → Nazwa → Flavor → Transpozycja → Na Mapie → Mechanika
  jako Opowieść → Źródła → Podsumowanie); brak bloku „Metryka i Kontekst
  Świata”, sekcji „Ilustracja”/„Druk w Kolekcji”; Fabuła cytowana jako
  kotwica osadzenia w Transpozycji i Źródłach (ADR 0026, pkt 4); cytowania
  z URL (Scryfall, MTG Wiki ×2); pinezka z uzasadnieniem lore (region,
  P4, bruk przy Tin Street — spójne z Fabułą i map.json). ✓
- **Migracje 1LTR i 2BFZ** pod LORE-first: nowe 9 sekcji, kronika na
  początku, mechanika przed Źródłami; testy ujemne wykluczają meta-tekst
  i treść promptu. Ton zgodny z ADR 0015 (lore, nie meta). ✓
- **Plan Ravnica**: Geografia/Ludy/Setting w pigułce/Źródła; graf
  sąsiedztw precyktów spójny z `mapa-analiza.md` (MA1); świadoma mieszanka
  epok udokumentowana („Era Kolekcji” + `otwarte_na_kolejne_przejscia`).
  ✓ (słabość jakościowa: 0 wikilinków — patrz Znalezione Z1)
- **Pogłębienie Zendikaru** (warstwa 1): sekcje Bala Ged i Sejiri z
  cytowaniami (MTG Wiki, mtglore.com, MTG Salvation) — lore, nie meta;
  spójne z kanonem (Umung = rzeka, Riverroot, Surrakar Caves, Midnight
  Pass, Ikiral, Benthidrix…). ✓
- **ADR 0030/0031** + rejestr (README) + status ADR 0016 „Częściowo
  zastąpiona” — spójne (pilnuje `test/rejestr-adr.test.js`). ✓
- **Dzienniki**: co-nowego (3 wpisy z nagłówkiem ADR 0029 `RRRR-MM-DD
  HH:MM`), PROJECT_HISTORY (3 wpisy sesji), ROADMAP K7 (PR-13 + v2 +
  kolejka v3). ✓ (drobiazgi: Z3/Z4)

## Kod — zgodny z ADR-ami

- **`src/codex/registry.js`**: `SEKCJE_KARTY` = lista ADR 0030 (9 sekcji);
  komentarz ADR zaktualizowany. ✓
- **`tools/mapforge/bloki.mjs`**: `escXml` w `etykieta()` i
  `lukEtykieta()` (fix: `&` w „Korozda & Svogthos” psuło SVG); klocki
  miejskie (`dzielnica`, `granicaDzielnicy`, `mur`, `szczelina`,
  `tkanina`, `gruz`, POI: plac/kolumny/kopula/platforma/kołowrót/most/
  ognisko/drzewo-poi) — deterministyczne (`prng(id)`), atlas w szarościach
  R=G=B (test motywu), komentarze z uzasadnieniem. ✓
- **`tools/mapforge/render.mjs`**: dedupe krawędzi dzielnic (klucz =
  posortowana para wierzchołków, `toFixed(1)`), kolejność warstw
  (tinty dzielnic POD biomami; granice NAD biomami, POD drogami/POI),
  `buforPas` jako strefa zajęta szczeliny, `ocean(..., kolor)` dla atlasu
  miejskiego (tło papier, nie morze). ✓
- **`tools/map-audit.py`**: `strefyWodne` z `scena.json` dołączane do
  whitelisty (jedno źródło prawdy w scenie); awaria parsowania sceny =
  problem, nie cisza. ✓
- **Testy**: ui-smoke (licznik kart 2→3, kontrakty ADR 0030: `Kronika
  Lore`, brak `Metryka i Kontekst Świata`, pozycja mechaniki), fixture
  1tst i wiki-stats pod nowym szkieletem; regresjaZendikaru — re-render
  bajtowo identyczny (handoff, potwierdzone testem suite). ✓
- **Zero zależności (ADR 0002)**: brak nowych pakietów; mapforge = klocki,
  nie ręczny `<path>` w treści. ✓
- **Proweniencja map (ADR 0013/0031)**: `map.json` (wariant, zrodlo,
  kalibracja, rekonstrukcja), `mapa-analiza.md` (MA1 z odrzuceniami T1/T2
  z uzasadnieniem licencyjnym), `zrodlo-transkrypcja-ggr.md` (źródło
  kalibracji v2), `zrodlo-fanowska-wektoryzacja.md` (kandydat v3). ✓

## Znalezione (kolejka napraw — bieżąca sesja)

| # | Co | Gdzie | Rozstrzygnięcie |
|---|---|---|---|
| Z1 | Plan Ravnica = najsłabsza strona bazy (7.0/8, **0 wikilinków**); sekcja „Ludy” jednoparagrafowa | `content/planes/ravnica.md` | S3/S4 (pogłębienie + hasła `boros`, `dziesiaty-dystrykt`) |
| Z2 | `zrodlo-fanowska-wektoryzacja.md` opisuje źródło jako JEDEN plik `TenthDistrict.png`; dostawa 2026-09-03 to **3 warstwy (a/b/c) + link Drive** | `maps/ravnica/` | S6 (aktualizacja pliku źródłowego) |
| Z3 | Literówki w opublikowanym dzienniku: „w **wariantie** T4” (→ „w wariancie”), „**Blisttercoils**” (→ „Blistercoils”) | `content/co-nowego.md` (wpis 2026-09-03 00:12) | S6 (korekta chirurgiczna) |
| Z4 | Pisownia „Blistercoils” w etykietach sceny i kotwicach mapy (kanon: Blistercoils) | `maps/ravnica/scena.json`, `map.json` | zweryfikowane w S5: sceny OK; literówka „Blisttercoils” tylko w dzienniku (Z3) |
| Z5 | `otwarte_na_kolejne_przejscia` (map.json): Gnat Alley, Guildpact Square + Pillar of the Paruns, Beacon Tower — do dopytania w kanonie | `maps/ravnica/map.json` | S5 (kwerenda z cytowaniem) |

## Werdykt

PR #13 **spójny z ADR-ami i bez regresji**; integralność zgodna z
handoffem. Znalezione Z1–Z5 nie są wadami blokadę stanowiącymi — trafiają
do kolejki bieżącej sesji (S3/S4/S5/S6).
