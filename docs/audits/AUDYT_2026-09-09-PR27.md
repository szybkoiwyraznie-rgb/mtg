# Audyt PR-27 (2026-09-09, sesja PR-28)

Zakres: `207463d..98b1f02` — PR-27 „audyt PR-25 i Pętla Jakości”
(47 plików, +2553/−250), scalony 2026-09-09.

Sesja audytująca: `arena/01a08651-mtg`.

## 1. Integralność bazy (stan PO PR-27)

- Klon sesji był płytki → `git fetch --unshallow origin` na starcie
  (ENVIRONMENT.md §2a, L11); build bez ostrzeżenia, stopki z pełnej historii.
- `npm test` **169/169** (handoff PR-27 podaje 166/166 — delta +3 to testy
  `widok_domyslny` z końcowych commitów PR-27, dopisane po zamrożeniu
  handoffu; drobny dryf L9 w dokumentacji, nie w kodzie).
- `npm run build` zielony — **30 stron (14 kart, 5 haseł, 11 planów)**,
  zgodnie z handoffem.
- `python3 tools/map-audit.py` = **0 problemów**; `node tools/wiki-stats.mjs
  --json` = **100% średniej kompletności** — zgodnie z handoffem.
- Brak zmian podkładów map (tylko `map.json`: pinezki + korekty FR),
  więc QA rastrowe nie było wymagane; wizja w tej sesji potwierdzona
  osobno (ogląd `maps/dominaria/l0.jpg`).

## 2. Przegląd grup plików

### 2.1. Nowe karty i snapshoty (556NPH, 309ISD)

Przejrzane: `collection/entries/{556nph,309isd}-*.md`,
`scryfall/{556nph,309isd}-*.json`,
`content/cards/{556nph-ruthless-invasion,309isd-civilized-scholar}.md`.

- Wpisy kolekcji: format 5-polowy z Fabułą, zgodny z dostawą — **OK**.
- Snapshoty: pełne, z `source`/`pobrano`/`slug` i notką o dwóch
  numeracjach (ADR 0036/L8) — **OK**. Snapshot 309ISD: `layout:
  transform`, dane frontu wyłącznie w `card_faces[0]`, top-level
  mirror celowo usunięty — dopuszczone przez ADR 0044 §4.
- Karty: szkielet 9 sekcji w kolejności (ADR 0030), głos Kronikarza
  (ADR 0042 — strażnik przechodzi), zero nawiązań do druku źródłowego
  (ADR 0040 — strażnik przechodzi), Fabuła jako kotwica Transpozycji
  i Na Mapie (ADR 0026), flavor EN + tłumaczenie + odczyt fraza po
  frazie zgodny ze snapshotem, Źródła z URL-ami.
- 309ISD nie wymienia drugiej twarzy z nazwy (Homicidal Brute/Human
  Mutant nie występują) — **zgodne z ADR 0044**; słowo „przemień”
  w Mechanice opisuje regułę własnej twarzy, nie drugą stronę.
- Pinezki: 556NPH `region` w Oxiddzie z uzasadnieniem z Fabuły;
  309ISD `dokladna` na Havengulu (miasto nazwane wprost w przekazie
  sceny) — **OK** (MA4).

### 2.2. Hasła (oxidda-chain, thraben, wybrzeze-mieczy) i pogłębienie Mephidrossu

Przejrzane: `content/lore/{oxidda-chain,thraben,wybrzeze-mieczy,mephidross}.md`
+ wikilinki w kartach 488SOM/393DKA/3CLB i planach mirrodin/innistrad/forgotten-realms.

- Oxidda Chain: próg 2 kart (488SOM + 556NPH) — **OK**. Thraben: próg
  2 kart (393DKA + 309ISD) — **OK**. Szkielet hasła, źródła z URL-ami,
  brak pinezki, odsyłanie `?x=&y=` (ADR 0043) — **OK**.
- Mephidross: pogłębienie lore (nimy, Moriokowie, Darkslick, Blackcleave,
  Rey-Goor, Ish Sah) z nowymi cytowaniami; usunięte słabe źródło
  „drukowane, cytowane przez zapis mapy” — **OK**.
- Wybrzeże Mieczy: treść, linkowanie i źródła dobre, ale próg
  formalnie niespełniony — znalezisko **F1** niżej.
- Drobiazg redakcyjny w 488SOM — znalezisko **D1** niżej.

### 2.3. Naprawy Forgotten Realms

Przejrzane: `content/planes/forgotten-realms.md`,
`maps/forgotten-realms/map.json`, errata w `maps/_warsztat/`,
`content/cards/3clb-nefarious-imp.md` (Źródła).

- F1/P1 i F2/P2 z audytu PR-25 naprawione wprost: Luruar w Silver
  Marches, Inner Sea = Sea of Fallen Stars, Lake of Steam, kierunek
  Morza Bezludnego; kotwice i notki z proweniencją; root-link
  `worldaneil.github.io` usunięty (przy okazji także słabszy wpis
  Official Bestiary — zmiana na lepsze) — **OK**.
- Follow-up po feedbacku właściciela: usunięte `poi` z map.json,
  warstwa `mapa-poi` i martwy wpis legendy z silnika, opis „czysty
  raster” na stronie planu, doprecyzowanie „nic nie doklejamy” w ADR
  0038 i PETLA_JAKOSCI — spójne w kodzie, teście i docs — **OK**.

### 2.4. Kod i testy (DFC per-face, widok_domyslny, regresje)

Przejrzane: `tools/content-loader.mjs`, `tools/build.mjs`,
`src/codex/render-map.js`, `src/codex/style.css`, testy `pokrycie-scryfall`,
`ui-smoke`, `mapy`, `mapa-warianty`, `artefakt`.

- `widokScryfallDlaKarty`/`znajdzTwarzScryfall`: pola twarzy z
  `card_faces[]`, wspólne z top-level — implementacja ADR 0044 — **OK**.
- `widok_domyslny`: walidacja [0,1]/zoom>0, pierwszeństwo deep-linków,
  przeliczenie przez kalibrację sceny — implementacja ADR 0045 — **OK**.
- Testy: walidacja snapshotów DFC per twarz + regresja 309ISD, regresja
  legendy (brak „obwódka regionu”), brak `mapa-poi` na FR, asercje
  mini-map warunkowe rasterizerem, 3 testy `widok_domyslny` — **OK**.
- Zero-dependency silnika nienaruszone (resvg/pngjs/jpeg-js tylko dev).

### 2.5. Dokumentacja (ADR 0044/0045, gidy, dziennik, historia, backlog)

Przejrzane: ADR-y 0044/0045 + README rejestru, AGENTS.md, SZKIELET_KARTY,
PROCES_MAP (nowe definicje T2/T3/T4), PETLA_JAKOSCI, maps/README,
`content/co-nowego.md`, `docs/PROJECT_HISTORY.md`, `docs/backlog.md`,
`docs/research/RESEARCH_2026-09-09-kaladesh-gearsmith-prodigy.md`,
handoff PR-27.

- ADR 0044/0045: decyzje właściciela, spójne z implementacją — **OK**.
- Research Kaladeshu: uczciwa drabina T1–T4 z negatywnym wynikiem T1/T2
  i rozstrzygnięciem właściciela (jedna mapa T4 + ognisko Ghirapur) —
  **OK**; to wejście do PR-28.
- `co-nowego`: nagłówki z godziną (ADR 0029), treść zgodna ze stanem — **OK**.
- Backlog: progi Oxidda/Thraben odnotowane poprawnie; jawny zapis progu
  Wybrzeża jako „karta + plan” (patrz F1) — **OK jako dokumentacja**.
- PROJECT_HISTORY: wpis PR-27 kompletny, ale edycja uszkodziła starszy
  wpis PR-21 — znalezisko **F2** niżej.

## 3. Znalezienia

| # | Priorytet | Opis | Dotknięte pliki | Kierunek naprawy |
|---|---|---|---|---|
| F1 | P2 | **Hasło `wybrzeze-mieczy` powstało poniżej progu 2 kart.** SZKIELET_HASLA wymaga „co najmniej 2 kart” odwołujących się do encji; tu jest 1 karta (3clb) + plan. Backlog PR-27 jawnie tak to zapisuje („Próg spełniały 3clb + plan”), więc to świadome odchylenie, nie przeoczenie — ale reguła nie została zmieniona. | `content/lore/wybrzeze-mieczy.md`, `docs/backlog.md` | **Nie kasować**: treść dobra, linkowanie i źródła poprawne, PR scalony przez właściciela. Odnotować jako zaakceptowane przez scalenie odchylenie jednorazowe; reguła progu 2 kart pozostaje bez zmian. |
| F2 | P3 | **PROJECT_HISTORY: zdublowany i urwany blok wpisu PR-21** (nagłówek sesji + pkt 1 powtórzone, zdanie urwane na „przeną map + Pętla Jakości”). Regresja edycyjna PR-27 — w `207463d` wpis był czysty. | `docs/PROJECT_HISTORY.md` | Usunąć zdublowany blok w PR-28 (naprawa w tej sesji). |
| D1 | nit | **488SOM: wikilink Oxidda bez pogrubienia** („gór [[oxidda-chain\|Oxidda Chain]]” zamiast `**[[…]]**`, jak w reszcie link-miningu). | `content/cards/488som-carapace-forger.md` | Dopisać pogrubienie w PR-28 (naprawa w tej sesji). |

## 4. Werdykt

PR-27 jest **mocny merytorycznie i kodowo**: naprawy FR wyprostowały
geografię, obie nowe karty trzymają LORE-first/Kronikarza/hierarchię
kanonu, obsługa DFC i `widok_domyslny` są czyste i regresyjnie
pokryte, a research Kaladeshu daje uczciwe wejście do PR-28. Znaleziska
to jedno świadome odchylenie progowe (F1, bez naprawy treści) i dwie
drobne naprawy redakcyjne (F2, D1) — wykonywane w PR-28.
