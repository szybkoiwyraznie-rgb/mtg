# Audyt startowy — PR-18 (audyt PR-17 + FF 275FIN/Midgar + Alara 305ARB) — 2026-09-05

Sesja `arena/01a0737d-mtg`, punkt wyjścia = scalony PR-18 (`4bc1234` = squash
sesji PR-18 na `main`). Audyt wg AGENTS.md §5: weryfikacja poprawności
i logiki zmian (treści i kodu), nie raport z testów.

## Integralność (punkt wyjścia)

- `npm test`: **104/104 zielone** (zgodnie z handoffem PR-18).
- `npm run build`: zielony — 10 stron (5 kart, 0 haseł, 5 planów); drzewo map
  w `dist/maps/`; ZIP = 11 plików (zgodnie z opisem PR-18).
  Uwaga: płytki klon gita w tej sesji → build ostrzega o błędnych datach
  „utworzono" (ADR 0029); nieblokujące, koryguje `fetch --unshallow`.
- `python3 tools/map-audit.py`: **RAZEM PROBLEMÓW: 0** (wszystkie mapy
  łącznie z warsztatem i podmapą Midgaru — zgodnie z handoffem).

## Zakres PR-18 (48 plików)

Cztery nurty: (A) audyt PR-17 + naprawy Z1–Z8 + Pętla Jakości (S1–S8),
(B) pakiet Final Fantasy — plan-franczyza, podmapa Midgar, karta 275FIN
(P1–P8), (C) pakiet Alara — karta 305ARB, plan i mapa Alary (P9–P10),
(D) silnik podmap `plan/podmapa` (loader/build/map-audit/router/render).

## Ocena — zgodne z ADR (bez zastrzeżeń)

1. **Szkielet kart LORE-first (ADR 0030):** obie nowe karty (275FIN, 305ARB)
   mają dokładnie 9 sekcji w wiążącej kolejności (Kronika Lore → Postacie
   i Byty → Nazwa Karty → Flavor Text → Transpozycja → Na Mapie → Mechanika
   jako Opowieść → Źródła → Podsumowanie Lore); brak sekcji „Ilustracja"/
   „Druk w Kolekcji"/„Narracja Kolekcji" (zweryfikowane linia po linii).
2. **Snapshopy Scryfall (ADR 0004):** oba kompletne (`name`, `mana_cost`,
   `type_line`, `oracle_text`, `set`, `rarity`, `artist`, `image_uris.normal`)
   + `source`/`pobrano`/`slug`; `notka_numery` dokumentuje dwie niezależne
   numeracje (imgId ≠ collector_number) — zgodnie z lekcją L8.
3. **Wpisy kolekcji (ADR 0003/0026):** nienaruszalne, Fabuła verbatim
   po frontmatterze; format dostawy v3 (imgId · nazwa · set · plan · FABUŁA).
4. **Mapy (ADR 0031/0032/0033):** proweniencja jawna w `map.json`
   (referencje fanowskie poza gitem, transkrypcja własna topologii,
   `rekonstrukcja: true`); pinezki z `karta` + `pewnosc` (obie `dokladna`
   z uzasadnieniem lore — budynek Shinra / Maelstrom); nazwy wyłącznie
   kanoniczne. Rejestr podmap (`plan/podmapa`) — loader/build/audit pokryte
   testem `podmapy.test.js` i rozszerzonym `mapy.test.js`.
5. **Taxonomia (ADR 0005):** nowe tagi (`szpiedzy`, `ekspedycje`, `demony`,
   `iluzje`, `maelstrom`) są w słowniku `content/taxonomia.json`.
6. **Wikilinki:** rozwiązują się (test 104), brak martwych linków i
   self-linków.
7. **Co nowego (ADR 0029):** nagłówki `## RRRR-MM-DD HH:MM — tytuł`,
   test schematu zielony.
8. **Silnik:** opcja `zrośnięte`, klocek `granicaRegionu`, podmapy — kod
   bez zależności (ADR 0002), determinizm zachowany (rng z hasha).

## Znaleziska (kolejka napraw bieżącej sesji)

**Z1 [TREŚĆ, wysoki] Termin „Fabuła dostawy" w widocznej treści nowych kart.**
ADR 0026 „Doprecyzowanie (2026-09-05)" (spisane w tym samym PR-18, S3)
zakazuje w widocznej treści kart terminu „Fabuła dostawy" — ma być samo
„Fabuła". Obie nowe karty go łamią:
- `content/cards/275fin-aerith-rescue-mission.md:71` („fabułę dostawy"),
  `:81` („Fabuła dostawy (ADR 0026)");
- `content/cards/305arb-illusory-demon.md:74` („Fabuła dostawy"),
  `:82` („Fabuła dostawy (ADR 0026)").
**Naprawa:** zamienić na „Fabuła"/„fabuła" (sens zdań bez zmian) — ten sam
wzorzec dryfu co w PR-17 (tam naprawiony w S16C), tutaj odtworzony przy
nowych kartach.

**Z2 [TREŚĆ, średni] Odsyłacze do mechaniki Codexu w treści kart.**
Feedback B (test ui-smoke: „treść nie może odsyłać do mechaniki Codexu")
zabrania „ADR" w treści karty. Nowe karty to łamią:
- 275FIN `:81` („(ADR 0026)");
- 305ARB `:81` („(ADR 0033…"), `:82` („(ADR 0026)"), `:100` i `:143`
  („(ADR 0033)").
**Naprawa:** przepisać bez odsyłaczy ADR (np. „jedna mapa scalonego planu"
zamiast „(ADR 0033: jedna mapa…)"); sama treść jest dobra, etykiety
procesowe są zbędne.

**Z3 [TEST, średni] Luka pokrycia ui-smoke dla nowych kart.**
Asercje `!includes('Fabuła dostawy')`, `!includes('ADR')`, `!includes('verbatim')`
są przypięte wyłącznie do 1LTR (linie 261–267) i 2BFZ (linia 314); 305ARB
sprawdzany tylko pod kątem imgId/tytuł/deep-link (147–150), a 275FIN tylko
w liście kart (sortowanie). Stąd Z1/Z2 przeszły zielono mimo naruszeń ADR.
**Naprawa:** dodać dla 275FIN i 305ARB te same asercje (najlepiej pętlą po
wszystkich kartach, żeby kolejna materializacja nie powtórzyła luki).

**Z4 [NISKI] Snapshot 275FIN zapytany niestandardowo.**
`source` = `search?q=!"Aerith Rescue Mission"&unique=prints` zamiast
`cards/named?exact=…` (ADR 0004 pkt 2). Wynik poprawny (właściwy print),
uwaga kosmetyczna — nie wymaga zmiany snapshotu.

**Z5 [STYL, niski] Strony planów cytują ADR w treści.**
`content/planes/alara.md` („(ADR 0033)" w akapicie, „(ADR 0031…)" w sekcji
Mapa) i `content/planes/final-fantasy.md` („(ADR 0032)" w leadzie) odsyłają
do mechaniki Codexu wprost; starsze plany (ravnica/srodziemie/zendikar)
tego nie robią. Plany nie mają testu tej zasady (dotyczy kart), więc to
obserwacja stylu, nie naruszenie testu — do rozstrzygnięcia, czy ujednolicić.

## Wnioski

PR-18 bez wad blokujących: integralność w pełni zielona (testy, build,
map-audit 0), zakres udokumentowany, karty LORE-first, snapshoty i mapy
z czystą proweniencją. Główny problem to **powtórzony dryf treści wobec
ADR 0026 doprecyzowania** (Z1) i wobec zasady „bez mechaniki Codexu
w treści karty" (Z2), który umknął, bo test pilnuje tych reguł tylko na
dwóch starych kartach (Z3). Kolejka napraw Z1–Z3 (+ opcjonalnie Z5) to
pierwszy blok pracy bieżącej sesji — **wstrzymany przed Pętlą Jakości,
czeka na bezpośrednie zlecenie właściciela**.
