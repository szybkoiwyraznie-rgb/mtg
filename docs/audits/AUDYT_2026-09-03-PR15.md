# AUDYT — 2026-09-03 — PR-15 (Ravnica v3 — finalizacja wektoryzacji T2+)

Audyt poprzedniego scalonego PR przed nową pracą (AGENTS §5). Sprawdza stan
repo PO PR-15 względem stanu na starcie (koniec PR-14).

## Zakres PR-15

PR-15 (gałąź `arena/01a06700-mtg`) dokończył wektoryzację mapy Ravnica do
**wariantu T2+** (decyzja właściciela): `maps/ravnica/{map.json,podklad.svg}`,
`mapa-analiza.md`, plan; usunął artefakt T4 `scena.json`.

## Integralność bazy

- `npm test` → **102/102 zielone** (zgodnie z handoffem PR-15).
- `npm run build` → zielone; artefakt 306 kB; drzewo map
  `dist/maps/*.html` (ravnica 346 kB, srodziemie 2130 kB, zendikar 3821 kB).
- `python3 tools/map-audit.py ravnica` → **0 problemów** (4 notki info
  o kotwicach w wodzie dla obiektów wodnych/pod-ulicznych — świadome).

## Zgodność z ADR

- ADR 0031: rastery źródłowe (a/b/c.png) poza gitem (gałąź robocza patch-3),
  proweniencja w `map.json` (zrodlo, kalibracja, silnik). Zgodne.
- ADR 0007/0013: `wariant: T2`, `rekonstrukcja: false` — podkład adoptowany,
  proweniencja w danych nie na obrazie. Zgodne.
- ADR 0027: mapa jako osobna strona HTML (`maps/ravnica.html`, 346 kB). Zgodne.
- Mapa nie jest bramkowana przez `mapforge.test.js` ani `map-audit` w CI —
  stan maskowany, nie oznacza pełnej poprawności (świadoma luka, patrz niżej).

## Merytoryczność / spójność danych

- Kotwice (56) pokrywają precynkty i POI; pinezka 137gpt (Withstand) obecna
  z uzasadnieniem Fabuły (ADR 0026) i proweniencją ADR 0031.
- map.json + mapa-analiza spójne; zrodlo-fanowska-wektoryzacja.md dokumentuje
  warstwy a/b/c.

## Wnioski / luki (dla bieżącej sesji)

1. **QA jakości wizualnej nie domknięte.** PR-15 dopiął strukturę i audyt,
   ale zgłoszenia właściciela (po oglądzie) o jakości — etykiety pasa
   Undercity, doczepianie markerów do POI, „Promenada do góry nogami”,
   kompletność dróg — dotyczyły stanu PO PR-15 i są tematem kolejnej sesji
   (PR-16). Odzyskany patch-6 już częściowo je rozwiązuje.
2. Brak bramki QA mapy Ravnica w CI — rozważyć (decyzja właściciela / osobny ADR),
   bo testy 102/102 przechodzą nawet przy mapie niedomkniętej wizualnie.
3. Odzyskany `podklad.svg` (patch-6) jest iteracją dalej niż scalony w PR-15 —
   niesie poprawki (etykiety Undercity, obrót promenady) ORAZ dryf pozycji dróg
   do naprawy. Baza pracy PR-16 = odzyskany podkład (decyzja właściciela).

## Rekomendacja

Przejść do PR-16 (plan: `docs/plans/PLAN_2026-09-03-pr16-ravnica-v3-qa.md`),
pracując na odzyskanym podkładzie z PR-15 jako rescuer baseline.
