# Handoff — 2026-09-06 — PR #20: audyt PR-19 + kolejka napraw A1–A4

## Stan

- Gałąź: `arena/01a076ea-mtg`; PR: #20 (otwarty, do Squash and merge przez
  właściciela). Punkt wyjścia: scalony PR-19 (`2a26531` = squash sesji
  PR-19 na main, 2026-09-06 15:30).
- Wszystkie commity wypchnięte; `npm test` **104/104**; `npm run build`
  zielony (10 stron: 5 kart, 0 haseł, 5 planów; ZIP 11 plików);
  `python3 tools/map-audit.py` = **RAZEM PROBLEMÓW: 0**.
- Klon pogłębiony (`git fetch --unshallow`) — pełna historia dla stopek
  czasu (ADR 0029).

## Zlecenie / tryb sesji

Właściciel: „Jedziesz. AGENTS i robisz dokładnie to co masz tam napisane"
(bez nowej dostawy materializacji). Praca: audyt poprzedniego scalonego
PR (AGENTS.md §5) + kolejka napraw + Pętla Jakości (ADR 0006).

## Zrobione (commity)

- **`025c41f`** — roadmapa sesji: `docs/plans/PLAN_2026-09-06-pr20-audyt-pr19-i-kolejka-napraw.md`.
- **`f515b32`** — audyt PR-19: `docs/audits/AUDYT_2026-09-06-PR19.md`.
  Zakres PR-19 (diff `4bc1234..2a26531`, 14 plików) = audyt PR-18
  (dokument) + **naprawy Z1–Z5** (deb7cc51) + **rewamp mapy Alary v2**
  (b1904dd8, po recenzji właściciela). Wnioski: naprawy Z1–Z5 poprawne
  i kompletne (termin „Fabuła", brak odsyłaczy ADR w kartach/planach,
  pętla ui-smoke po wszystkich kartach, snapshoty `cards/named?exact=`);
  rewamp Alary v2 zgodny z ADR 0031/0033 i kanonem nazw (28 POI,
  40 kotwic, klocek `wir` w mapforge); pinezka 305ARB zweryfikowana
  point-in-polygon w środku wiru Maelstromu.
- **`ad8f507`** — A1: `content/planes/alara.md` do stanu mapy v2
  (Esper = archipelag na Morzu Esper z akwenami Dwindling Sea/Sea of
  Stars/Inkwell; Maelstrom = równoprawny region-węzeł; pełna lista POI
  wg mapy; Źródła + Esper + Category:Alara locations).
- **`4461e07`** — A2: wpis co-nowego o scalonym PR-19 (2026-09-06 15:30).
- **`b8d2f82`** — A3: wpis PROJECT_HISTORY o sesji PR-19.
- **`a829d42`** — A4: ROADMAP — PR-18/PR-19 oznaczone jako scalone.
- **`707adaa`** — L9 w `docs/LESSONS.md`: opis PR aktualizowany
  kumulatywnie po każdym commicie merytorycznym (pułapka z PR-19:
  opis wymieniał tylko commit audytowy, reszta zakresu scalona
  „po cichu").
- **`6d39020`** — wpis co-nowego PR-20 (2026-09-06 15:50).

## Pętla Jakości — wynik

- Krok 1 (integralność): zielone (104/104, build, map-audit 0).
- Krok 2 (pogłębianie): brak kandydatów — wszystkie strony 8/8
  (wiki-stats), Źródła ≥2, sekcje kompletne, pinezki obecne.
- Krok 3 (link-mining): brak encji wspominanych przez ≥2 karty (5 kart
  w 5 różnych planach). Kolejki haseł w `docs/backlog.md` czekają na
  drugą kartę danego planu.
- Krok 4 (pass mapowy): pinezki 5/5, mapy 5/5 planów z kartami
  (zweryfikowane skryptem: każda karta ma pinezkę we właściwym
  `map.json`); rewamp Alary v2 sprawdzony w audycie (spójność
  map.json↔scena.json, etykiety, XML podkładów, geometria pinezki).
  Uwaga warsztatowa: mapa Alary v2 to **obiekt odniesienia dla stylu
  „archipelag + węzeł-wir"** — klocek `wir` jest teraz w warsztacie
  mapforge (BLOKI_BIOMOW), reużywalny dla przyszłych planów.

## Uwagi / pułapki dla następnej sesji

- **Sesja PR-19 nie domknęła dokumentacji** (brak handoffu, co-nowego,
  PROJECT_HISTORY; opis PR nieaktualizowany) — uzupełnione w tej sesji
  (A2/A3/L9). Następna sesja: aktualizować opis PR **po każdym commicie**
  (L9), nie na końcu.
- **PR-20 do scalenia przez właściciela (Squash and merge)**; po scaleniu
  opis PR-20 już zawiera pełny zakres (kumulatywnie).
- Możliwe przyszłe kierunki TYLKO na zlecenie: kolejna karta FF (nowa
  podmapa części sagi), pogłębienie lore FF/planu, pogłębianie Alary
  (strona planu świeżo zaktualizowana do mapy v2 — dobry punkt startu
  dla haseł Alary przy drugiej karcie), Ravnica v3 (czeka na rastry
  właściciela — procedura w `maps/ravnica/zrodlo-fanowska-wektoryzacja.md`).
- **Vision w tej sesji: brak** (read_file obrazów zwraca „no vision
  capabilities") — QA map prowadzono programistycznie (XML, map-audit,
  point-in-polygon, spójność rejestrów). Przyszła sesja z wizją może
  obejrzeć mapę Alary v2 wzrokowo (rewamp po recenzji właściciela nie
  był oglądany przez agenta).
- Zakaz generowania grafik (ADR 0008) bez zmian; `img/` poza gitem.
- Preview (jeśli potrzebne): `python3 -m http.server 8000 --bind 0.0.0.0
  --directory dist` (katalog `dist/` zbudowany w tej sesji).
