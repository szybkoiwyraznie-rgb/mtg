# Handoff — 2026-09-06 — PR #21: audyt PR-20 z recenzją wizualną map + Pętla Jakości + pakiet 2 (488SOM / Mirrodin T4)

## Stan

- Gałąź: `arena/01a0770f-mtg`; PR: #21 (otwarty, do Squash and merge przez
  właściciela). Punkt wyjścia: scalony PR-20 (`bbe6d34`, 2026-09-06 16:11).
- Wszystkie commity wypchnięte; `npm test` **107/107** (3 nowe testy
  `map-audit`); `npm run build` zielony (10 stron: 5 kart, 0 haseł,
  5 planów); `python3 tools/map-audit.py` = **RAZEM PROBLEMÓW: 0**
  (z nową regułą `TYTUŁ NA OBIEKCIE`); `npm run stats` 10/10 stron 8/8.
- Klon pogłębiony (`git fetch --unshallow`).

### Aktualizacja po pakiecie 2 (23:30)

- HEAD gałęzi = commit zamykający pakiet 2 (po `3c4cf6b`), **wypchnięty**;
  `npm test` **107/107**; `npm run build` zielony (**12 stron: 6 kart,
  0 haseł, 6 planów**); `python3 tools/map-audit.py` = **0** (6 map,
  w tym nowa `mirrodin`). Opis PR #21 zaktualizowany kumulatywnie (L9)
  o pakiet 2 i tytuł.
- **Incydent środowiska:** w trakcie pakietu 2 sandbox został odświeżony
  (klon płytki `bbe6d34`, gałąź lokalna bez historii PR-21, `/tmp`
  i pliki poza repo utracone, `GH_TOKEN` nieważny ~2 h). Praca trwała
  lokalnie na commicie-odtworzeniu `adf2ac8` (drzewo identyczne
  z origin `4ba8173`); po odzyskaniu tokena: `git fetch` →
  `git rebase --onto FETCH_HEAD adf2ac8` → push fast-forward (bez force).
  Lekcja: **commitować i pushować po każdym kroku merytorycznym** —
  właściciel słusznie zwrócił uwagę, że przez ~2 h nie było żadnego
  commita; przy utracie tokena commitować lokalnie od razu, nie czekać.

### Aktualizacja po pakiecie 3 (2026-09-07 ~03:00) — Tarkir / 509KTK

- Commity pakietu 3: `ba59e38` dostawa + snapshot · `a316f1e` research
  T2→T3→T4 · `e7b633d` mapforge `pustynia` + POI `szczyt` + dzielnice
  bez granicy · `442ee98` mapa T4 Tarkiru · `b1431ed` karta + plan +
  taksonomia + smoke · `395f0f4` co-nowego · (ten dopisek + roadmapa +
  ADR 0033 + PROJECT_HISTORY/ROADMAP w kolejnym commicie).
  **Token GitHub wygasł w trakcie pakietu** (drugi raz w sesji) —
  praca szła na lokalnych commitach; po odświeżeniu tokena środowisko
  wróciło jako **płytki klon na `bbe6d34` z pracą jako niezacommitowanym
  patchsetem** (ENVIRONMENT §2a). Procedura: `git fetch --unshallow`,
  `git reset a316f1e` (stan origin), odtworzenie łańcucha commitów
  w tej samej kolejności, push po każdym. Stare lokalne SHA
  (`362ef7a`, `b9104c5`, `b03f799`, `1cfa2d7`, `7714735`) nie istnieją.
- `npm test` **109/109**; `npm run build` zielony (**14 stron: 7 kart,
  0 haseł, 7 planów**); `python3 tools/map-audit.py` = **0** (7 map,
  w tym nowa `tarkir`). Podgląd :8000 zbudowany po `395f0f4`.
- **Mapa Tarkiru:** T4 na geometrii rastra fanowskiego Lore Café /
  MTG Wiki Italia (3d4, 2025) dostarczonego przez właściciela w czacie
  (plik nie dotarł do sandboxa — `/home/user/uploads/` nie istnieje,
  jak w PR-14; odczyt z podglądu w UI). Raster = źródło pomocnicze
  geometrii (ADR 0031), kanon rozstrzyga; jedna korekta wobec rastra
  (Temur nie graniczy z Abzanem). Zmiany wyłącznie przez
  `tools/mapforge/tarkir-scena-t4.py` → `cli.mjs`; współrzędne rastra
  przelicza funkcja `R(px, py)` (1568×1208 → 2000×1400).
- Do zrobienia po sesji: recenzja właściciela mapy Tarkiru; opis PR #21
  o pakiet 3 (kotwica: sekcja „Pakiet 3 (w toku)” → zamienić na
  „Pakiet 3” z commitami); test `parosc-kolekcji` znów zielony.

## Zlecenie / tryb sesji

Właściciel: „Kontynuuj zgodnie z AGENTS” (bez nowej dostawy
materializacji) — audyt poprzedniego scalonego PR + Pętla Jakości.
**Pierwsza sesja, w której agent oglądał obrazy** (rastry map).

## Zrobione (commity)

- **`927da3d`** — roadmapa: `docs/plans/PLAN_2026-09-06-pr21-audyt-pr20-i-petla-jakosci.md`.
- **`7b74fbc`** — audyt PR-20: `docs/audits/AUDYT_2026-09-06-PR20.md`.
  PR-20 bez wad (A1–A4 poprawne, L9 zastosowana). Recenzja wizualna
  (resvg poza repo, 1800 px całość + cropy z 4000 px): Alara v2 dobra co
  do topologii/stylu, ale **W1–W3** — tytuły „Jund”/„Grixis”/„Naya” na
  forcie, paśmie, szlaku i rzece; Zendikar bez regresji; Midgar OK;
  Ravnica (T2) czysta. B1 = brak wpisu PR-20 w PROJECT_HISTORY/ROADMAP;
  B2 = luka `map-audit` (nie widział tytuł↔obiekt).
- **`e5f0d8b`** — W1–W3: `maps/alara/scena.json` — Naya 430/700→285/580,
  Jund 620/1190→800/1040, Grixis 1260/1090→1320/1230 (skan siatki: box
  tytułu vs POI ±30 / pasma / 7 punktów na lądzie; potwierdzone na cropach).
  Podkład zregenerowany `cli.mjs`; kotwice regionów w `map.json` bez zmian
  (dopisek w `otwarte_na_kolejne_przejscia`).
- **`2c09978`** — B1: wpis sesji PR-20 w `docs/PROJECT_HISTORY.md`
  i `docs/ROADMAP.md` (+ PR-21).
- **`b90d101`** — B2: `tools/map-audit.py` reguła **`TYTUŁ NA OBIEKCIE`**
  (box tytułu `tytul-kontynentu`/fs ≥ 40 + margines 6 vs kotwice
  `mf-fort/miasto/ruina/iglica/wulkan/szczyt/hedron/wodospad/herb`;
  biomy i wir poza listą — ADR 0025). Kalibracja: stara Alara → dokładnie
  W1–W3; wszystkie bieżące mapy → 0. Nowy `test/map-audit.test.js`:
  **map-audit w `npm test`** + fixtura pozytywna/negatywna; skrypt
  przyjmuje ścieżkę bezwzględną katalogu. SKILL_MAPA_PLANU §10.
- **`e8eb18c`** — krok 2 Pętli: `content/planes/alara.md` sekcja
  **„Odłamy i ludy”** (5 akapitów: Bant/Esper/Grixis/Jund/Naya; mtg.wiki,
  5 URL w Źródłach); `docs/backlog.md` — kolejka link-miningu Alary.
- **`5a958c3`** — `docs/setup/ENVIRONMENT.md` §1a (rasteryzacja i ogląd
  map: convert bez SVG, resvg-js poza repo, wizja do sprawdzenia na
  starcie); `docs/LESSONS.md` **L10** (geometria nie zastępuje oka).
- (ten commit) — co-nowego 17:30, handoff, PROJECT_HISTORY (PR-21),
  checkboxy planu.

## Pętla Jakości — wynik

- Krok 1: zielone (104→107/107, build, map-audit 0).
- Krok 2: **Alara pogłębiona** (jedyny plan MTG bez sekcji o ludach;
  „Setting w pigułce” 2 zdania; najstarsza z remisu 8/8). Pozostałe
  strony bez słabości wg rankingu.
- Krok 3: brak encji w ≥2 kartach (5 kart w 5 planach; sprawdzone
  skryptem po pogrubieniach) — haseł nie tworzono; kolejki w backlogu
  (Śródziemie, Zendikar, Ravnica, **Alara — nowa**).
- Krok 4: pass mapowy = recenzja wizualna + W1–W3 + B2. Pinezki 5/5.

## Pakiet 2 — 488SOM Carapace Forger + plan Mirrodin (zrobione)

Zlecenie właściciela po pakiecie 1: „pora na kolejną kartę i zupełnie
nowy plan do narysowania” (488SOM · Carapace Forger · SOM · Mirrodin
+ Fabuła). Decyzje z czatu: T4 od razu z kanonu; raster fanowski
(inkarnate „Mirrodin”, J. Ruder — niedostępny z sandboxa) tylko gdy
właściciel go dostarczy i tylko jako źródło pomocnicze dla pozycji
nieustalonych; pakiet w PR #21; Fabuła verbatim.

- **`a165f15`** — roadmapa `docs/plans/PLAN_2026-09-06-pr21-mirrodin-carapace-forger.md`.
- **`b130e66`** — `collection/entries/488som-carapace-forger.md` (Fabuła
  verbatim), `scryfall/488som-carapace-forger.json` (SOM/114),
  `tools/mapforge/mirrodin-scena-t4.py` (generator sceny, deterministyczny),
  `maps/mirrodin/{scena.json,podklad.svg,map.json,zrodlo-research.md}`.
  Mapa: tarcza-półkula metalowej sfery (C=(1000,710), R=590 na arkuszu
  2000×1400), ocean = papier `#e9e9e9`, Glimmervoid centralny, pięć
  regionów po 72° wg cyklu fastlandów SOM, Rey-Goor przy krawędzi,
  lacuny = `hedron`, słońca jako `nota()` poza tarczą, `kompas: false`,
  `skala: false` (silnik honoruje). 28 POI, 38 kotwic
  (`pozycja_zrodlo` relacyjne), `regiony: []`, `rekonstrukcja: true`,
  epoka MRD–SOM przed kompleacją (ADR 0033 §2), pinezka 488SOM `region`
  w sercu Tangle (0.2837, 0.376).
- **`35ea659`** — `content/planes/mirrodin.md` (`mapa: mirrodin`),
  `content/cards/488som-carapace-forger.md` (9 sekcji ADR 0030),
  `content/taxonomia.json` (+`elfy`, `phyrexia`, `rzemioslo`),
  `test/ui-smoke.test.js` (asercje Mirrodinu; licznik 6; home = najnowsza
  materializacja — lista pokazuje 5, więc 1LTR z niej wypadł).
- **`3c4cf6b`** — co-nowego 23:20 + checkboxy roadmapy.

Ogląd rastra (L10) wymusił trzy poprawki w generatorze: pasma Oxiddy
skrócone i tytuł „Oxidda Chain” przeniesiony (leżał na szczycie — złapał
to też `map-audit`), Black Lacuna odsunięta od tytułu „Mephidross”,
etykiety pograniczy (fastlandy) jawnie w bordo (kotwiczone w bagnie/lesie
dostawały zieleń biomu). Rasteryzacja wycinków: `viewBox`-crop wywala
resvg (panic) — używać opcji `crop` resvg (skrypt w ENVIRONMENT §1a
warto uzupełnić, patrz niżej).

## Uwagi / pułapki dla następnej sesji

- **Wizja:** sprawdź na starcie jednym `read_file` na PNG. Jeśli jest —
  po każdej zmianie tytułów/POI/pasm rasteryzuj (`/tmp`, `@resvg/resvg-js@2`,
  skrypt z SKILL_MAPA_PLANU §8) i oglądaj całość + cropy (L10). `/tmp`
  znika z sesją — wnioski do audytu.
- **`map-audit` jest teraz w `npm test`** — czerwony audyt = czerwony
  pakiet. Margines reguły `TYTUŁ NA OBIEKCIE` = 6; przy 14+ Zendikar
  dawałby fałszywe alarmy (Ondu × ruina 555/1130) — nie podnosić bez
  recenzji rastrów.
- Audyt nadal nie widzi tytułu nad drogą/rzeką ani kompozycji — to oko.
- Mapa Alary v3 = v2 + typografia tytułów; scena deterministyczna
  (`cli.mjs` odtwarza podkład bajt w bajt).
- PR-21 do scalenia przez właściciela (Squash and merge); opis PR
  kumulatywny (L9) — aktualny po każdym commicie.
- Możliwe kierunki TYLKO na zlecenie: kolejna karta (druga karta Alary
  uruchamia kolejkę haseł z backlogu), Ravnica v3 (czeka na rastry
  właściciela).
- Preview (jeśli potrzebne): `python3 -m http.server 8000 --bind 0.0.0.0
  --directory dist`.
- **Mirrodin:** mapa jest rekonstrukcją relacyjną — przy kolejnej karcie
  planu NIE przesuwać kotwic bez nowego źródła; rozbieżność wiki („Tangle
  graniczy z Quicksilver Sea”) vs cykl fastlandów opisana w
  `zrodlo-research.md` i `otwarte_na_kolejne_przejscia`. Sceny z ery
  New Phyrexia/Mirrex wymagają osobnego podkładu (ADR 0033 §2).
  Zmiany mapy wyłącznie przez generator `mirrodin-scena-t4.py` →
  `cli.mjs` (nigdy ręcznie w `scena.json`/SVG).
- **Wycinki rastra:** resvg-js 2.x panikuje przy podmianie `viewBox`
  na wycinek; działa opcja `crop: {left,top,right,bottom}` przy
  `fitTo: {mode:'zoom', value: s}`.
- **Stopki dat i „Co nowego” w podglądzie (zgłoszenie właściciela
  2026-09-06 ~23:50):** to nie była regresja kodu — podgląd był zbudowany
  z płytkiego klonu po odświeżeniu sandboxa (daty = jedyny widoczny
  commit) i sprzed ostatniego wpisu co-nowego. Naprawa: `git fetch
  --unshallow` + świeży build; wnioski w ENVIRONMENT §2a/§6/§7 i L11.
  Kontrola: Coralhelm Guide „Utworzono 2026-08-31 20:38 · ostatnia
  aktualizacja 2026-09-05 16:44”.
- **Audyt PR-21 (następna sesja)** powinien objąć oba pakiety: audyt
  PR-20/W1–W3/B2 oraz Mirrodin (mapa T4, karta 488SOM, strona planu,
  taksonomia) — plus incydent środowiska (commity lokalne przez ~2 h).

### Aktualizacja po recenzji mapy Tarkiru (2026-09-07 ~14:10)

- Właściciel: 4 uwagi (ramka nachodzi na treść; The Scour = „pogięta
  rura”; lądolód zasłania pasmo; rzeki kończą się w polu). Naprawa
  w silniku + generatorze: `aa76360` (ramka `passePartout`, klocek
  `rozpadlina`, `pasmoInstancje({wyklucz})` = poligony lodu,
  `sprawdzHydrologie` w `sprawdzWiazania`, pełna sieć rzek Tarkiru,
  Zendikar: rzeka Bala Ged → Umung, demo: Srebrna → morze; testy +4 =
  **113/113**; map-audit 0) · `cd186ad` ADR 0034 + SKILL_MAPA_PLANU
  pułapka #6/checklista + LESSONS L12 · (ten dopisek + co-nowego 14:05
  + roadmapa w kolejnym commicie).
- **Token GitHub wygasł TRZECI raz** (po `ade4a2f`) — commity lokalne;
  po odzyskaniu: `git push origin arena/01a0770f-mtg` (fast-forward) i
  dopisek do opisu PR #21 (sekcja „Recenzja mapy Tarkiru”). Jeśli
  środowisko wróci jako płytki klon z patchsetem — procedura §2a
  (`fetch --unshallow`, `reset` na stan origin, odtworzenie łańcucha).
- Otwarte: potwierdzenie epoki etykiet Tarkiru (nazwy khanów, ADR 0033).
