# Handoff PR-25 (2026-09-08)

Gałąź `arena/01a081d0-mtg`, **PR #25 otwarty** (opis kumulatywny, L9).
Poprzednia sesja (PR #24: Dominaria T1 + podmapa Aerony) zawiesiła się po
pierwszej wersji mapy; ten PR to recenzja właściciela + naprawy.

## Stan

- **159/159 testów**, build 22 strony (11 kart, 1 hasło, 10 planów),
  map-audit 0, ZIP 90,5 MB (201 plików).
- Commity: `477c6ca` plan · `088ac93` audyt PR-24 · `83d4a65` ADR 0040 ·
  `db4cd22` ADR 0041 + Uzupełnienie 0039 · `d74f71b` naprawy treści +
  strażnik `druk-zrodlowy` · `77d227d` mapa (L2 usunięte, pinezka M1,
  build czyści dist) · `21c819a`+`5b152d4` dokumentacja · `ec22cbf` L15.

## Co się zmieniło (sedno)

1. **Procedura (ADR na sztywno):** ADR 0040 — oryginalna ilustracja karty
   NIE jest kanonem (kanoniczne tylko FOT/KON właściciela); jakiekolwiek
   nawiązywanie do printu i wnioskowanie z niego zabronione. Strażnik:
   `test/druk-zrodlowy.test.js` (ilustracj*/wignet/artysta ze snapshotu
   w `content/cards/*.md` = błąd). Karta 40USG przepisana z Fabuły
   właściciela + kanonu; Forge Devil bez nazwiska artysty.
2. **Mapa Dominarii:** decyzja właściciela — podmapa będąca wycinkiem bazy
   bez nowego detalu nie ma zastosowania (ADR 0041). Usunięto nakładkę L2
   „Domeny” + `aerona.jpg` (1,6 MB). Mapa = L0 `l0.jpg` + kafle L1
   (176×512 px, prog 2.5×). Pinezka 40USG zmierzona na M1 master
   (px 1569,1979 → 0.1937,0.3806; pewność `region`).
3. **Build:** `zbudujPakiet` czyści katalog wyjściowy (L15) — pliki
   usunięte z repo znikają z `dist/` i ZIP-a; test regresyjny.

## Otwarte dla właściciela

- **Lorwyn w ZIP-ie** (~17,5 MB = 2×8,3 MB SVG inline, ~47% objętości):
  optymalizacja (T2/T1 zamiast T4 albo kompresja) — osobna decyzja.
- Odpowiedź na pytanie o ZIP (dlaczego strony map są duże, czego nie
  dublują) — w opisie PR § „Odpowiedź na pytanie o ZIP”.

## Weryfikacja

`npm test` (159) + `npm run build` (22 strony) + `python3 tools/map-audit.py`
(0). Preview: `dist/` na porcie 8000 (serwer statyczny). Mapę Dominarii
sprawdzić na zoomie ≥2.5× — kafle wchodzą stopniowo, bez nakładki
deep-zoom.

## Uwaga na przyszłe sesje

Reguły L15 i ADR 0040/0041 są trwałe: nie wnioskować z ilustracji kart,
nie tworzyć podmap-wycinków, pełny build zawsze czyści katalog wyjściowy.
