# HANDOFF 2026-09-01 — po sesji PR-4 (Pages + mapy B1/B2 + Pętla Jakości)

> Handoff = skrót JEDNEJ sesji. Zasady trwałe żyją w AGENTS.md, ADR-ach i
> ENVIRONMENT.md — w razie rozbieżności wygrywa repozytorium.

## Stan projektu

- **PR #7** (`arena/01a05bc9-mtg` → `main`) otwarty, **merge umówiony
  na koniec sesji z właścicielem** (Squash and merge). Zadania sesji:
  A (Pages), B1+B2 (mapy), C (Pętla Jakości) — wykonane.
- **Pages włączony** przez właściciela (site istnieje, `build_type:
  workflow`, `https://szybkoiwyraznie-rgb.github.io/mtg/`). **Pierwsza
  publikacja odpali się przy scaleniu PR #7** — naturalny trigger
  `on: push` do main; dalej każdy push do main publikuje automatycznie.
- **Pętla Jakości v2 (ADR 0015)**: krok 2 = pogłębianie LORE
  (anti-lista: artyści/wydruki/meta — ADR 0014 usunął sekcję „Druk
  w Kolekcji"); krok 4 = kompletacja i jakość map (POI, weryfikacja,
  warsztat rysowania, wspólny silnik T4 — ROADMAP K7). Dawnego kroku 4b
  nie ma (wchłonięty przez krok 4).
- `npm test` = 70/70; `npm run build` OK; `npm run stats`: karty 88%,
  plany 63% (jedyna brakująca oś to wikilinki — czekają na hasła,
  tj. na drugą kartę planu; próg ≥2 kart).

## Co zrobiono w tej sesji (commity na gałęzi)

1. **Setup + diagnoza Pages (A):** plan, audyt PR #6, L6/ENVIRONMENT §3,
   ADR 0013 (dryf ADR 0012 ↔ mapa — naprawiony). Pages włączony ręcznie
   przez właściciela; bot nie ma uprawnień `workflows`/`actions: write`,
   więc nie pushuje zmian workflow ani nie odpala runów (fakty stałe).
2. **B1 — badge pinezki:** ukryty do najechania (`:hover`/`:focus-visible`,
   tooltip `title` zostaje). Test dymny pilnuje reguł CSS w artefakcie.
3. **B2 — warstwa karty z pinezki:** kliknięcie pinezki otwiera Kartę
   Katalogową na zmaksymalizowanej warstwie nad mapą (`role="dialog"`,
   `aria-modal`); zamknięcie ✕ / klik w tło / Esc; fokus wraca do mapy;
   mapa NIE jest odmontowywana, więc zoom/pan zostają. Renderery
   wstrzykiwane z `main.js` (`renderKarty`, `zamontujKarte`) — brak
   cyklu importów. Progressive enhancement: pinezka to nadal zwykły
   link `#/karta/…` (Ctrl/Cmd+klik = nowa karta); deep-link `?pin=`
   wyśrodkowuje jak dotychczas.
4. **C — Pętla Jakości (ADR 0006, potem v2 wg ADR 0015):**
   - krok 1 integralność: 70/70 + build zielone na każdym commicie;
   - krok 2 pogłębienie lore: plan Zendikar — nowa sekcja **Ludy**
     (rasy, rody wampirów Guul Draz, korowie, Zulaport) wg
     *Planeswalker's Guide* + MTG Wiki + kopie mtglore; naprawa reguły
     cytowań w 2BFZ (zniknął wpis „wiedza ogólna bez URL-a");
     *[korekta właściciela w trakcie sesji: pogłębione początkowo
     biografie artystów i sekcja „Druk w Kolekcji" — USUNIĘTE, ADR 0014]*;
   - krok 3 link-mining: potwierdzony brak encji wspólnych dla ≥2 kart
     (karty z różnych planów) — hasła dalej zablokowane progiem;
   - krok 4 pass mapowy: pinezki i mapy kompletne; od ADR 0015 krok 4
     to czynna kompletacja map (POI, weryfikacja, warsztat T4) — kolejna
     sesja zaczyna go realnie wykonywać;
   - krok 5: co-nowego, ROADMAP (K4 + K7), PROJECT_HISTORY, niniejszy
     handoff, opis PR zaktualizowany kumulatywnie.
- Incydent środowiskowy: reset workspace w trakcie sesji (ENVIRONMENT §2)
  — odzyskano z gałęzi zdalnej; wszystkie commity pushowane od razu.

## Dopisek (ta sama sesja, zlecenia A/B)

- **A (Pages bez merge'a):** bot nie odpali workflowu (403). Właściciel
  może w UI: Actions → „Publikacja na GitHub Pages” → failed run →
  **„Re-run all jobs”** — site już istnieje, workflow na main poprawny,
  więc re-run publikuje bez scalania PR. Alternatywa: scalenie PR #7
  (trigger `on: push`). Podgląd sandboxowy dist/ serwowany na porcie
  8000 w trakcie sesji.
- **FOT/KON:** zakodowane i przetestowane (ADR 0008) — przyciski torów
  Druk/FOT/KON sondują `./img/<imgId>FOT|KON.png`; cichy fallback;
  działają też w warstwie karty z mapy (B2). Wersja lokalna = artefakt
  + katalog `img/` obok.
- **ADR 0016 — format Wpisu Karty (kanon katalogowy):** po audycie
  szablonu właściciela. Sekcja 1 z blokiem danych Oracle (wpis
  samowystarczalny), mechanika w 3 warstwach (odczyt zasadniczy →
  interpretacja → całość jako opowieść; podtypy jako warstwy), flavor
  fraza po frazie + kontekst cytującego, polskie odczytanie nazwy,
  podsumowanie tezami. **Zakaz sekcji „Ilustracja”** (i „Druk
  w Kolekcji” — ADR 0014): obraz Scryfalla wyłącznie w infoboksie.
  Retrofit 1LTR/2BFZ wykonany; asercje w ui-smoke pilnują kontraktu.

## Dopisek 2 (ta sama sesja: FOT/KON w treści + mapa Zendikaru a–j)

- **ADR 0017:** FOT (panorama) rysuje się nad treścią karty, KON pod
  pierwszą sekcją — sloty sondowane, cichy fallback; przyciski torów
  usunięte (ADR 0008 częściowo zastąpiony).
- **Mapa Zendikaru:** 10 zgłoszeń właściciela (a–j) + 7 z audytu
  skryptowego naprawione (AUDYT_...-mapa-zendikar-feedback.md).
  Kanonowe korekty: Valakut = superwulkan Akoum (MTG Wiki), Singing
  City na Murasie (Plane Shift), Zulaport na Beyeen (Plane Shift),
  Crypt of Agadeem na wyspie Agadeem. Weryfikator: spłaszczanie
  Bezier + PIT + bbox kolizje (wnioski w audycie → warsztat T4).
- map.json: 70 kotwic; elementy Beyeen/Valakut rozdzielone.

## Dopisek 3 (ta sama sesja: pełna Pętla Jakości)

- **Krok 2 (LORE):** Tharbad → srodziemie.md; Murasa wg Guide →
  zendikar.md (źródła dopisane).
- **Krok 3 (link-mining):** encje wspólne poniżej progu ≥2 kart — brak
  nowych haseł; utkano wikilinki plany↔karty (6 linków).
- **Krok 4 (mapa):** przekład wnętrza Murasy pod kanon Guide (Na Plateau
  i Singing City na wschodzie); nowe kotwice z cytowaniami; Living Spire
  zarejestrowany; `tools/map-audit.py` jako standard weryfikacji
  (0 problemów na obu mapach); SKILL_MAPA_PLANU §10.
- **Krok 5:** wiki-stats — pinezka planu = pinezki kart na jego mapie;
  completeness 100% (4/4 strony 8/8).
- Pełny raport: `docs/audits/AUDYT_2026-09-01-petla-jakosci.md`.

## Dopisek 4 (ta sama sesja: silnik mapowy mapforge)

- **ADR 0018 + `tools/mapforge/`** (geom/bloki/render/cli, testy w
  `test/mapforge.test.js`): scena JSON → warstwowy SVG w pergaminie,
  deterministyczny (PRNG z hasha id; fix: `.reverse()` mutował zwracane
  krawędzie wstęgi). Demo: `maps/_warsztat/podklad.svg` (audit 0).
- Testy pakietu: 81/81 (11 nowych); runner łapie tylko `.test.js`.
- Adopcja: E1 (scena Zendikaru z danych) → E2/E3 (rendery próbne
  i migracja) — `docs/plans/PLAN_2026-09-01-mapforge.md`; nowy element
  mapy = klocek (SKILL §11). Demo warto obejrzeć oczami właściciela
  (jakość ≥ Śródziemie to ocena wzrokowa).
- map-audit: etykiety textPath (bez x/y) pomijane; runner `_warsztat`
  w codziennym audycie.

## Dopisek 5 (ta sama sesja: motywy pergamin/atlas)

- Silnik dostał palety jako dane: `motyw('pergamin'|'atlas')` + flaga
  `--styl=` w CLI; atlas = tusz na papierze, linie wody przybrzeżne,
  bez plam oceanu (README mapforge: „Motywy"). Demo A/B:
  `maps/_warsztat/podklad.svg` vs `podklad-atlas.svg`.
- map-audit: czyta wszystkie `podklad*.svg` planu (prefiks pliku
  w komunikatach); zbiór fill lądu rozszerzony o paletę atlasową.
- CLI: fix parsowania `--demo --styl=… plik.svg` (flaga zjadała ścieżkę).
- Decyzja o motywie docelowym (i ewentualny ADR 0019 amending palety
  ADR 0008) — u właściciela po obejrzeniu A/B.

## Dopisek 6 (ta sama sesja: ADR 0019 — czysty czarno-biały)

- Właściciel po A/B wybrał monochromatyczny **czysty czarno-biały**
  (nie sepia) — wzorzec mapome. Motyw `atlas` przepisany na tryb
  `tusz`: ląd = biały papier, korony drzew i szczyty konturem,
  cieniowanie haćurą (klocki: drzewo/szczyt/wulkan/oczka bagnien
  rozgałęzione na tryb), woda = linie wody; jedynie detale w szarości
  (pęknięcia lodu, dym). Utrwalone w ADR 0019 (atlas = domyślny
  dla map planów T4; pergamin zostaje alternatywą; kolor = tylko
  warstwy funkcjonalne, np. pinezki).
- `maps/_warsztat/podklad-atlas.svg` wygenerowany na nowo (audyt 0,
  testy 82/82; asercja dominacji kreski tuszowej).
- Następny krok: E1/E2 planu adopcji — scena Zendikaru z danych
  i render próbny w atlasie.

## Kolejna sesja

1. Lektura startowa (AGENTS.md §0); najnowszy handoff = ten plik.
2. **Najpierw:** jeśli PR #7 scalony — zweryfikować deploy
   (`gh run list --workflow=pages.yml` zielony; strona
   `https://szybkoiwyraznie-rgb.github.io/mtg/` renderuje się) oraz
   obejrzeć B1/B2 na żywo (mapa Śródziemia: hover badge, klik pinezki →
   warstwa karty → ✕ wraca do mapy w tym samym zoomie).
3. Bez nowej dostawy kart → Pętla Jakości v2 (PETLA_JAKOSCI.md):
   - pogłębienie LORE: plany 63% (Śródziemie nie było pogłębiane —
     kandydat: ludy zachodniego Śródziemia, uważając na anty-dublowanie
     z przyszłymi hasłami dunland/isengard/Saruman); NIE meta-informacje
     (ADR 0014/0015);
   - **druga karta na plan** odblokuje hasła (backlog: kolejki encji
     dla Śródziemia i Zendikaru);
   - pass mapowy (krok 4 v2): research nowych POI, weryfikacja
     dokładności, warsztat rysowania T4 (ROADMAP K7; SKILL_MAPA_PLANU
     jako pamięć metod); benchmark = mapa Śródziemia.
4. Właściciel może skasować stare gałęzie po sesjach (`arena/01a05755-mtg`,
   `arena/01a0591f-mtg`, `patch-*`) i podbić akcje w workflowach do v5
   (backlog; wymaga jego uprawnień).

## Pułapki / obserwacje

- **Nie pushować zmian w `.github/workflows/`** ani nie odpalać runów
  — brak uprawnień bota (ENVIRONMENT §3); naturalny trigger deployu
  to push/scalenie do main.
- **Warstwa karty (B2) zależy od wstrzykniętych rendererów** — jeśli
  zmieni się sygnatura `zamontujMape`, aktualizuj też `main.js`
  (wywołanie z `renderKarty`/`zamontujKarte`) i jsdoc w render-map.js.
- Shim DOM w testach nie obsługuje querySelector → interakcje warstwy
  są testowane asercjami statycznymi (szkielet dialogu + CSS w artefakcie);
  pełna weryfikacja UX — na artefakcie u właściciela.
- Sandbox resetuje workspace także w środku sesji (§2) — pushować
  po każdym zielonym commicie (działało).
