# Handoff PR-29 (2026-09-09)

Gałąź `arena/01a0874a-mtg`, **PR #29 otwarty**:
https://github.com/szybkoiwyraznie-rgb/mtg/pull/29

Roadmapa sesji:
`docs/plans/PLAN_2026-09-09-pr29-audyt-pr28-i-petla-jakosci.md`.

## Stan

- **176/176 testów, 0 pominiętych** (pełne środowisko: python3,
  ImageMagick, manifest Dominarii), build **31 stron (15 kart,
  4 hasła, 12 planów)**, `python3 tools/map-audit.py` = **0
  problemów**, `node tools/wiki-stats.mjs` = **100% średniej
  kompletności** (kaladesh 8.0/8).
- Sesja objęła cztery etapy:
  1. audyt scalonego PR-28 (`docs/audits/AUDYT_2026-09-09-PR28.md`)
     — werdykt: powyżej standardu; 5×P3 (N1–N5), wszystkie naprawione;
  2. Pętla Jakości, krok 2: pogłębienie strony Kaladeshu (Aetherflux
     Reservoir, detal dzielnic, sekcja „Kultura i codzienne życie”,
     jalpari);
  3. Pętla Jakości, krok 3: link-mining — bez nowych haseł
     (kolejka Kaladeshu cała o 1 kartę od progu; L17, test
     `prog-hasel`);
  4. Pętla Jakości, krok 4: pass mapowy (T4 własna) — Aetherflux
     Reservoir na płycie L2 Ghirapuru, nowy klocek mapforge
     `zbiornik` (SKILL_MAPA_PLANU pkt 1 + README katalogu).

## Dla następnej sesji

- PR #29 do scalenia przez właściciela; po merge standard:
  roadmapa → PR → audyt PR-29 → dostawy.
- Kolejki link-miningu (obie poniżej progu 2 kart, L17):
  - Kaladesh (backlog): Ghirapur, Konsulat/mistrzowie Konsulatu,
    Greenwheel/„Zoo”, eter/rdzeń eterowy, Targ Wynalazców — każda
    o 1 kartę; Iglica Eteru/Peema/Vahd/Lathnu/Vinday/Vasavati liczą
    się od kart, nie od planu.
  - Forgotten Realms (backlog): Wybrzeże Mieczy, Avernus/Baator,
    imp, Wojna Hobgoblinów/Tiamat — po 1 karcie.
- Era Avishkar/DFT (Omenpathy, Aetherdrift) = osobna mapa + ewent.
  osobna strona planu na żądanie karty; Aetherflux Reservoir już
  na płycie L2 i stronie planu.
- L16 obowiązuje: wpis verbatim natychmiast po dostawie.
- Sandbox Areny (sprawdzone w sesji): `dist/` i `node_modules` nie są
  utrwalane między turami — build na start sesji (31 stron, ZIP
  ~87,8 MB), podgląd `python3 -m http.server --directory dist 8000`;
  mini-mapy idą pełną bazą (brak rasterizatora w sandboxie — fallback
  `build.mjs`, stan zgodny z ROADMAP/PR-28); unshallow weryfikować
  `git rev-list --count HEAD` + `git cat-file -e <rodzic>` (flaga
  `is-shallow` potrafi kłamać po cichym fetchu).
