# Handoff — 2026-09-03 — PR #17: Ravnica v4 — domknięcie mapy T2+ do złotego standardu

## Stan

- Gałąź: `arena/01a067e0-mtg`; PR: #17 (otwarty, do Squash and merge przez właściciela).
- Punkt wyjścia: scalony PR-16 (`56ceae6` = main).
- Wszystkie commity wypchnięte (token GitHub odnowiony w trakcie sesji —
  wcześniej S5 padło na wygasłym tokenie, ale praca była bezpieczna w S1–S4
  na zdalnej; workspace był zresetowany i odzyskany wg ENVIRONMENT §2).
- `npm test` 102/102; `npm run build` zielony; `python3 tools/map-audit.py ravnica` = **0 problemów**.

## Kluczowa nowość tej sesji: AGENT MA WIZJĘ (vision)

Poprzednie agenty (PR-14/15/16) pracowały **bez oglądu obrazów** — analizowały
rastry programistycznie (maski, detekcja nasyconych pikseli). Ta sesja
**widzi obrazki** (narzędzie read_file na PNG zwiera widoczny obraz). To
pozwoliło domknąć mapę bezpośrednim porównaniem render↔źródło. Render SVG→PNG:
`sharp` (npm) zainstalowany w `/home/user/qawork/` (POZA repo — zero-deps
projektu nienaruszone, ADR 0002). Warsztat (skrypty + kadry + źródłowe PNG)
żyje w `/home/user/qawork/` i jest poza gitem (ADR 0031: raster źródłowy
nigdy w repo).

## Złoty standard

3 warstwy na gałęzi `szybkoiwyraznie-rgb-patch-3` (commit `59dc7e0`),
`maps/ravnica/{a,b,c}.png` (6849×5292, 1:1 z viewBox SVG):
- **a** — kształty dzielnic (ciągła sieć przerywanych granic + ramka/baner/kompas),
- **b** — + biomu/struktury (szare poligony gildii, arterie),
- **c** — + POI (kolorowe markery) i etykiety.
Rastry wyciągnięte z obiektu gita do `/home/user/qawork/src/` (nie w checkoutcie).

## Zrobione (commity)

- **S1** `dcf252b` — plan v4 (`docs/plans/PLAN_2026-09-03-pr17-*.md`) + audyt PR-16.
- **S2/S3** `d88a25b` — przebudowa markerów i etykiet wg c.png: etykiety POI
  czarne (białe tylko na pasie Undercity), zawsze pod markerem ze szczeliną;
  nazwy geograficzne bez kółek; ~15 fałszywych markerów usuniętych, brakujące
  dodane (podwójne Blistercoils, czarny Medori), rekolor Skarrg (Gruul) i
  Nivix (Izzet).
- **S2b** `0267176` — `tools/map-audit.py`: model kolizji etykiet AABB → **OBB/SAT**
  (Separating Axis Theorem); Ravnica 0, Śródziemie 0, Zendikar 4→2 (reszta
  pre-existing, poza zakresem).
- **S4** `9272ba3` — ciągła sieć granic dzielnic (7 połączonych odcinków)
  z oglądu a.png; styl `#4a4a4a`, dasharray 58/46.
- **S5/S6** `9ef6e2c` — konektor Millennial (długa jasnoszara przerywana linia
  platforma→węzeł granic), centrum (marker Vizkopy zdjęty z tekstu ORZHOVA),
  granica P6/P1 na wschód od KAMEN FORTRESS, SMELTING QUARTER pod granicę,
  dociągnięcie etykiet dużych gildii pod dyski, usunięcie martwej ścieżki drogi.
- **S7** `bcb2146` — dokumentacja: rozdział v4 w `mapa-analiza.md` + wpis
  w `content/co-nowego.md` (19:45) + ten handoff.

## Usterki właściciela — status

1. Granice dzielnic zniknęły → **domknięte** (ciągła sieć, S4 + korekty S5/S6).
2. Labelki za wysoko / wchodzą na markery → **domknięte** (pod markerami, S2/S3 + dociągnięcia).
3. Labelki w kolorze POI → **domknięte** (czarne; białe tylko na ciemnym Undercity).
4. Ulice/place mylone z POI → **domknięte** (geograficzne bez kółek, POI z markerami).
5. Konektor Millennial źle prowadził → **domknięte** (długa linia do węzła, S5/S6).
6. Kolizje etykiet (Deadbridge/Benzer i in.) → **domknięte** (Benzer bez markera,
   etykiety rozdzielone; audyt OBB/SAT = 0).
7. Wzór = źródłowe PNG → praca prowadzona cały czas 1:1 z a/b/c.

## Świadome różnice względem źródła (do decyzji właściciela)

- **Markery bez białych emblematów gildii.** W źródle duże markery mają biały
  symbol gildii w dysku (płomień, pięść, drzewo, smok…). Nasze to proste
  koncentryczne koła (dysk gildii + pierścień/środek akcentu). Właściciel nie
  zgłaszał tego jako usterki; dodanie emblematów to osobna praca (glify w kole).
- **Deadbridge Chasm:** w źródle MA mały zielony marker Golgari (właściciel
  opisał go jako punkt geograficzny). Zostawiony marker zgodnie ze źródłem
  (reguła 7: wzór to PNG); Benzer's Bridge bez markera (droga). Do potwierdzenia.
- Pisownia: źródło „TRANSGUILD PROMENADE", my „TRANS-GUILD PROMENADE" (bez dywizu
  w źródle). Drobiazg, można ujednolicić.

## Niezrobione / kolejka

- Preview w sandboxie: serwer statyczny `/home/user/qawork/serve.mjs` (port 8000)
  serwuje `dist/`; po przebudowie artefaktu odświeża mapę Ravnicy.
- Ewentualne emblematy gildii na markerach (decyzja właściciela).
- Dalsze mapy planów w kolejce Pętli Jakości; hasła Ravnicy czekają na 2. kartę.

## Uwagi procesowe

- Sesja = gałąź = PR #17; nie force-push; mergi należą do właściciela.
- Rastery źródłowe i warsztat renderu NIGDY do gita (ADR 0031; są w `/home/user/qawork/`).
- Przy pracy z `sharp`: `density` liczy skalę względem viewBox 6849×5292.
- Po resecie workspace odzysk: `git fetch origin arena/01a067e0-mtg && git reset --hard FETCH_HEAD`.
