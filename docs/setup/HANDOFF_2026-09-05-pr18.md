# Handoff — 2026-09-05 — PR #18: audyt PR-17 + Pętla Jakości

## Stan

- Gałąź: `arena/01a07207-mtg`; PR: #18 (otwarty, do Squash and merge przez
  właściciela). Punkt wyjścia: scalony PR-17 (`bd6ac14` = main).
- Wszystkie commity wypchnięte; `npm test` 102/102; `npm run build`
  zielony; `python3 tools/map-audit.py` = **RAZEM PROBLEMÓW: 0** (pierwszy
  raz łącznie z warsztatem).
- Klon lokalny pogłębiony (`git fetch --unshallow`) — pełna historia
  potrzebna stopkom czasu (ADR 0029); kolejna sesja dostanie świeży klon
  i ewentualnie znów powinna pogłębić, jeśli będzie pracować z datami.

## Zrobione (commity)

- **S1** — audyt startowy PR-17 (`docs/audits/AUDYT_2026-09-05-PR17.md`,
  znaleziska Z1–Z7) + plan sesji.
- **S2** — Z1: kompas „rosea" Ravniki (litery N/E/S/W z `y="undefined"`,
  wada od PR-15) + zapora map-audit na `undefined/NaN/null` w atrybutach.
- **S3** — Z2: doprecyzowanie ADR 0026 (termin „Fabuła" bez dopisku
  „dostawy", decyzja właściciela 2026-09-05) + rejestr ADR.
- **S4** — Z3: korekta numeracji ADR 0028→0027 (drzewo HTML map) w
  co-nowego i build.mjs.
- **S5** — Z5: demo warsztatu bez kolizji (etykieta „Rzeka Srebrna"
  w dół biegu) + świeży render demo (fort obecny od PR-11 tylko w scenie).
- **S6** — Z6/Z7 + Z8: nieaktualna notka w mapa-analiza.md, literówki
  docstringów, naprawa pozornej idempotentności `ravnica-v3-herby.py`
  (licznik głębokości zamiast non-greedy regexa; lekcja L7).
- **S7** — co-nowego (17:05), PROJECT_HISTORY, ten handoff.

Z4 z audytu WYCOFANE po weryfikacji kodu (modele geometrii etykiet w
map-audit są spójne — oba wyśrodkowane w kotwicy).

## Pętla Jakości — wynik

- Krok 2 (pogłębianie): brak kandydatów — wszystkie strony 8/8
  (`wiki-stats`), Źródła ≥2, sekcje kompletne, pinezki obecne.
- Krok 3 (link-mining): brak encji wspominanych przez ≥2 karty (3 karty
  w 3 różnych planach). Kolejka haseł Ravniki (backlog) czeka na 2. kartę
  z planu — jak przyjdzie dostawa z Ravniki, hasła ruszają.
- Krok 4 (pass mapowy): naprawy Z1/Z5 + przegląd kompletności
  (pinezki 3/3, mapy 3/3 planów z kartami, kotwice ravniki zgodne z
  warstwą SVG — sprawdzone przy audycie).

## Niezrobione / kolejka (bez zmian — decyzje właściciela)

- Globalna geometria Zendikaru („małe morze" Akoum–Ondu) — ROADMAP.
- Obwódki haseł (E5) — wymaga doprecyzowania zakresu.
- Ewentualne dalsze różnice Ravnika vs złoty standard — tylko z decyzją
  właściciela (T2+ adoptowany; rozszerzenia poza źródło za jego zgodą).
- Brak warsztatu renderu poprzedniej sesji (`/home/user/qawork` nie
  istnieje; gałąź `szybkoiwyraznie-rgb-patch-3` niedostępna) — gdyby
  trzeba było porównywać render z rastrem źródłowym, odtworzyć warsztat
  wg `maps/ravnica/zrodlo-fanowska-wektoryzacja.md` (rastery musi
  dostarczyć właściciel).

## Uwagi procesowe

- Sesja = gałąź = PR #18; nie force-push; mergi należą do właściciela.
- Skrypty warstw Ravniki (`ravnica-v3-herby.py`, `ravnica-v3-lokacje.py`)
  są teraz bajtowo idempotentne — po każdej ich zmianie warto to
  potwierdzić pustym `git diff` na podkładzie (lekcja L7).
- Nowa zapora map-audit (atrybuty undefined/NaN/null) działa na
  wszystkich mapach łącznie z grupami transformowanymi (etykiety/obiekty
  wewnątrz `<g transform=…>` nie są już poza zasięgiem tej klasy błędów).
