# Handoff sesji PR-32 — naprawy audytu PR-31 + 132GNT

- **Data:** 2026-09-11
- **Gałąź:** `arena/01a091f4-mtg`
- **PR:** #32 — <https://github.com/szybkoiwyraznie-rgb/mtg/pull/32>
- **Źródła zakresu:** `docs/audits/AUDYT_2026-09-11-PR31.md` oraz dostawa
  właściciela `132GNT / Pilgrim's Eye / GNT / Zendikar` z Fabułą.
- **Status:** wszystkie F1–F13 naprawione, 132GNT zmaterializowane;
  lokalne bramki końcowe zielone.

## Decyzja właściciela

Właściciel zatwierdził 2026-09-11 wariant mapy Eldraine **T4**. Wariant
został zachowany. Korekty zmieniają błędne twierdzenia, POI, pinezkę
209ELD i kompozycję, lecz nie zastępują T4 innym podkładem.

## Wykonane pakiety

1. **F1 / DFC (`3261701`):** usunięto dane innych fizycznych twarzy z
   118MID, 171ISD i 309ISD; dodano repo-szeroki strażnik ADR 0044.
2. **F2 / 347NPH (`b13f9b7`):** usunięto metadane konkretnego druku i
   nieźródłowe Razor Fields, kuźnię oraz bastion; pinezka ma poziom
   `przyblizona`; rozszerzono ADR 0040 i test globalny.
3. **F3–F5, F8–F9 / Eldraine (`1ff325a`):** zapisano zatwierdzenie T4;
   209ELD wróciło do Castle Ardenvale jako `dokladna`; globalna topologia
   jest jawnie umowna; Burning Yard jest jednym kompleksem Embereth,
   Locthwain mobilnym symbolem, a zaginiony Kocioł nie jest POI. Usunięto
   niepotwierdzone punkty i poprawiono trasę Vantress oraz kolizje.
4. **F6 / Alara (`cf6b40d`):** wycofano `Carmot Mines` i `Ruins of
   Vithia`; scena liczy znów 28 POI, a SVG odtwarza się deterministycznie.
5. **F7 / źródła (`8c30780`):** numery Confluxu jednoznacznie mapują się
   na cztery źródła; naturalne scalenie oddzielono od działań Bolasa;
   Civilized Scholar czyta o Stensii w Havengulu, bez wymyślonej podróży.
6. **F10–F13 (`bfe6038`):** handoff, historia, roadmapa i oba plany PR-31
   opisują finalne 15 materializacji i 57 stron; godziny ADR 0029
   odpowiadają commitom; dodano L19; poprawiono redakcję. Nieużywany
   `wiedzmin.jpg` z korzenia usunięto, a runtime LOD pozostał kompletny.
7. **132GNT (`1d6f695`, `13fad1c`, `bbefda2`):** Fabułę zapisano verbatim jako
   pierwszą operację; pełny snapshot właściwego druku GNT #55 rozdziela
   numer Scryfall od `imgId` właściciela. Powstała Karta Katalogowa
   Pilgrim's Eye o korowym thopterze-zwiadowcy, przybliżona pinezka przy
   Sea Gate, aktualizacja strony Zendikaru i changelogu. Fabuła nie
   nazywa kontynentu ani obozu, więc punkt jest operacyjnym indeksem
   sieci wypraw, nie dokładnym adresem sceny.

Dodatkowe testy regresyjne: `test/eldraine.test.js`, `test/alara.test.js`,
`test/proweniencja-lore.test.js`, `test/zamkniecie-pr31.test.js`,
`test/zendikar.test.js` oraz rozszerzone `test/pokrycie-scryfall.test.js`,
`test/druk-zrodlowy.test.js` i `test/ui-smoke.test.js`.

## Bramki końcowe po ostatnim commicie produktu i dokumentacji

- `npm test` — **194/194**, 0 pominiętych;
- `npm run build` — **58 stron** (34 karty, 9 haseł, 15 planów),
  **859 plików** drzewa;
- `python3 tools/map-audit.py` — **0 problemów**;
- `node tools/wiki-stats.mjs` — **100%**, średnia **7,7/8**;
- `git diff --check` — czysto;
- build nie zawiera usuniętego rootowego `wiedzmin.jpg`.

## Vision i preview

Vision jest dostępne i zostało użyte. Obejrzano świeże rastry 1600×1120:

- **Eldraine:** główne kolizje centrum, Embereth i Locthwain usunięte;
  Burning Yard nie nachodzi na góry, a droga Vantress kończy się na brzegu
  Lochmere;
- **Alara:** oba niepotwierdzone POI i ich glify zniknęły bez pozostawienia
  artefaktów kompozycyjnych.

Świeży build jest wystawiony w sandbox preview na porcie 8000; strona
główna, Karta Katalogowa Pilgrim's Eye i mapa Zendikaru odpowiadają
HTTP 200. Podkład Zendikaru nie był modyfikowany — doszła wyłącznie
pinezka karty; jej pozycję i opis potwierdzono w świeżym preview.

## Stan końcowy i następny krok

Brak otwartych znalezisk F1–F13 ani niedomkniętych elementów dostawy
132GNT. Po zielonym CI PR #32 jest gotowy do przeglądu i scalenia przez
właściciela.
