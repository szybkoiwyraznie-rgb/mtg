# Handoff sesji PR-32 — naprawy audytu PR-31 + 132GNT + 42ISD + 555DSK + 540DST

- **Data:** 2026-09-11–12
- **Gałąź:** `arena/01a091f4-mtg`
- **PR:** #32 — <https://github.com/szybkoiwyraznie-rgb/mtg/pull/32>
- **Źródła zakresu:** `docs/audits/AUDYT_2026-09-11-PR31.md` oraz dostawy
  właściciela `132GNT / Pilgrim's Eye / GNT / Zendikar`,
  `42ISD / Murder of Crows / ISD / Innistrad`,
  `555DSK / Bedhead Beastie / DSK / Wiedźmin` i
  `540DST / Chittering Rats / DST / Wiedźmin`, wszystkie z Fabułą.
- **Status:** wszystkie F1–F13 naprawione; 132GNT, 42ISD, 555DSK i
  540DST zmaterializowane; lokalne bramki końcowe zielone.

## Decyzja właściciela

Właściciel zatwierdził 2026-09-11 wariant mapy Eldraine **T4**. Wariant
został zachowany. Korekty zmieniają błędne twierdzenia, POI, pinezkę
209ELD i kompozycję, lecz nie zastępują T4 innym podkładem.

## Wykonane pakiety

1. **F1 / DFC (`3261701`):** usunięto dane innych fizycznych twarzy z
   118MID, 171ISD i 309ISD; dodano repo-szeroki strażnik ADR 0044.
2. **F2 / 347NPH (`b13f9b7`):** usunięto metadane konkretnego druku i
   nieźródłowe Razor Fields, kuźnię oraz bastion; pinezka ma poziom
   `przyblizona`; rozszerzono ADR 0040 i test globalny.
3. **F3–F5, F8–F9 / Eldraine (`1ff325a`):** zapisano zatwierdzenie T4;
   209ELD wróciło do Castle Ardenvale jako `dokladna`; globalna topologia
   jest jawnie umowna; Burning Yard jest jednym kompleksem Embereth,
   Locthwain mobilnym symbolem, a zaginiony Kocioł nie jest POI. Usunięto
   niepotwierdzone punkty i poprawiono trasę Vantress oraz kolizje.
4. **F6 / Alara (`cf6b40d`):** wycofano `Carmot Mines` i `Ruins of
   Vithia`; scena liczy znów 28 POI, a SVG odtwarza się deterministycznie.
5. **F7 / źródła (`8c30780`):** numery Confluxu jednoznacznie mapują się
   na cztery źródła; naturalne scalenie oddzielono od działań Bolasa;
   Civilized Scholar czyta o Stensii w Havengulu, bez wymyślonej podróży.
6. **F10–F13 (`bfe6038`):** handoff, historia, roadmapa i oba plany PR-31
   opisują finalne 15 materializacji i 57 stron; godziny ADR 0029
   odpowiadają commitom; dodano L19; poprawiono redakcję. Nieużywany
   `wiedzmin.jpg` z korzenia usunięto, a runtime LOD pozostał kompletny.
7. **132GNT (`1d6f695`, `13fad1c`, `bbefda2`):** Fabułę zapisano verbatim jako
   pierwszą operację; pełny snapshot właściwego druku GNT #55 rozdziela
   numer Scryfall od `imgId` właściciela. Powstała Karta Katalogowa
   Pilgrim's Eye o korowym thopterze-zwiadowcy, przybliżona pinezka przy
   Sea Gate, aktualizacja strony Zendikaru i changelogu. Fabuła nie
   nazywa kontynentu ani obozu, więc punkt jest operacyjnym indeksem
   sieci wypraw, nie dokładnym adresem sceny.
8. **42ISD (`14ab6f2`, `dad617c`):** Fabułę zapisano verbatim jako
   pierwszą operację; pełny snapshot właściwego druku ISD #70 oddziela
   collector number od `imgId` właściciela. Powstała dziewięciosekcyjna
   Karta Katalogowa Murder of Crows o krukach przechwytujących błękitne
   wspomnienia i ostatnie słowa nad pustymi szatami oraz srebrną tarczą
   strażnika. Pinezka `region` stoi na kotwicy Stensii; stare grafy przy
   Farbogach są kontekstem lore, nie zmyślonym dokładnym adresem sceny.
   Strona Innistradu opisuje teraz siedem kart.
9. **555DSK (`ddf824e`, `04ba588`):** Fabułę zapisano verbatim przed
   researchem; pełny snapshot właściwego druku DSK #125 jawnie rozdziela
   collector number od `imgId` właściciela. Karta Katalogowa Bedhead
   Beastie rozwija wojenną chatę w Velen, kudłatego beboka z posłaniem na
   rogach oraz dwóch redańskich milicjantów zwierających szyk w progu.
   Folklorystyczna nazwa pozostaje nazwą miejscowego strachu; podobieństwo
   do biesa jest ostrożnym porównaniem, nie identyfikacją gatunku. Pinezka
   `region` stoi na nowej kotwicy Velen (`0.4113/0.2807`, około
   `px_t1 [2095, 2024]`) i nie udaje dokładnego adresu anonimowej chaty.
10. **540DST (`76af35f`, `e0a8ef9`):** Fabułę zapisano verbatim przed
   researchem; pełny snapshot właściwego druku DST #39 rozdziela
   collector number od `imgId` właściciela. Karta Katalogowa Chittering
   Rats rozwija labirynt kanałów Novigradu, agresywne stada
   pseudoszczurów i poszukiwacza osłaniającego cenne mapy kosztem
   przerwania wyprawy. Karta przechodzi z ręki na wierzch biblioteki
   zamiast trafić na cmentarz, więc mechanika opowiada o zwłoce, nie utracie
   zdobyczy. Pinezka `dokladna` stoi na zweryfikowanej wizualnie ikonie
   Novigradu (`0.4045/0.2469`, `px_t1 [2060, 1780]`), ale nie wskazuje
   zmyślonego włazu ani korytarza. Pierwszy odczyt `2000/1700`, trafiający
   na wybrzeże na północny zachód od miasta, poprawiono po uwadze
   właściciela. Novigrad przekroczył próg dwóch kart i otrzymał
   hasło geograficzne bez osobnej pinezki. Strona Wiedźmina opisuje teraz
   trzy karty.

Dodatkowe testy regresyjne: `test/eldraine.test.js`, `test/alara.test.js`,
`test/proweniencja-lore.test.js`, `test/zamkniecie-pr31.test.js`,
`test/zendikar.test.js`, `test/innistrad-42isd.test.js`,
`test/wiedzmin-555dsk.test.js`, `test/wiedzmin-540dst.test.js` oraz
rozszerzone `test/pokrycie-scryfall.test.js`,
`test/druk-zrodlowy.test.js`, `test/prog-hasel.test.js`
i `test/ui-smoke.test.js`.

## Bramki końcowe po ostatnim commicie produktu i dokumentacji

- `npm run test:all` — **207/207**, 0 pominiętych;
- `npm run build` — **62 strony** (37 kart, 10 haseł, 15 planów),
  **853 pliki** drzewa archiwum;
- `python3 tools/map-audit.py` — **0 problemów**;
- `node tools/wiki-stats.mjs` — **100%**, średnia **7,7/8**;
- `git diff --check` — czysto;
- build nie zawiera usuniętego rootowego `wiedzmin.jpg`.

## Vision i preview

Vision jest dostępne i zostało użyte. Obejrzano świeże rastry 1600×1120:

- **Eldraine:** główne kolizje centrum, Embereth i Locthwain usunięte;
  Burning Yard nie nachodzi na góry, a droga Vantress kończy się na brzegu
  Lochmere;
- **Alara:** oba niepotwierdzone POI i ich glify zniknęły bez pozostawienia
  artefaktów kompozycyjnych.

Świeży build jest wystawiony w sandbox preview na porcie 8000; aplikacja
i mapa Wiedźmina odpowiadają HTTP 200. Build zawiera Karty Katalogowe
Bedhead Beastie i Chittering Rats, hasło Novigrad, obie najnowsze pinezki,
współrzędne kotwic Velen i Novigradu oraz deep-linki powrotne do kart.
Podkład T1 Wiedźmina nie został przerysowany — 555DSK dodało regionalną
kotwicę danych Velen i pinezkę, a 540DST wykorzystuje istniejącą kotwicę
miasta. Wcześniejsze kontrole Pilgrim's Eye, Murder of Crows oraz map
Zendikaru i Innistradu pozostają ważne.

## Stan końcowy i następny krok

Brak otwartych znalezisk F1–F13 ani niedomkniętych elementów dostaw
132GNT, 42ISD, 555DSK i 540DST. Po zielonym CI PR #32 jest gotowy do
przeglądu i scalenia przez właściciela.
