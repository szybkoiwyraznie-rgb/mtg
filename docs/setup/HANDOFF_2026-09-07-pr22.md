# Handoff PR #22 — 2026-09-07, punkt kontrolny po odnowieniu GitHuba

**Praca jest w toku. Audyt i naprawy są wykonane; pogłębianie lore
oraz link-mining w Pętli Jakości NIE są jeszcze wykonane.**

## Gałąź i stan

- Jedyna gałąź: `arena/01a07d33-mtg`.
- PR otwarty: https://github.com/szybkoiwyraznie-rgb/mtg/pull/22
- Właściciel wznowił połączenie GitHub i polecił wypchnąć zmiany.
  Zaległy A4 (`6a3622d`) oraz A5 (`1d2d4fd`) zostały wypchnięte.
- Ten checkpoint domyka dokumentacyjną część A6, nie całą Pętlę.
  Opis PR jest kumulatywny. Nie scalać — robi to właściciel.
- Pełna historia gita; **nie powtarzać bezwarunkowo `--unshallow`**.

## Wykonane

1. Roadmapa/PR na starcie: `2188033`.
2. Pełny audyt PR #21 (`bbe6d34` → `6bf2fba`, 61 plików): `2db0c56`.
   Raport `docs/audits/AUDYT_2026-09-07-PR21.md` opisuje stan PRZED
   naprawami. Nie przerabiać go na opis poprawionego stanu.
3. A1 — kanon Tarkiru: `68331ce`. KTK ≠ FRF, Karakyk ≠ Ayagor;
   First Tree przy Arashin; T4 bez późniejszych Ayagor/Melting Wilds/
   Aerie i powierzchniowego Dusyut. 23 POI, 61 etykiet, 14 biomów.
   Errata faktograficzna ADR 0033, bez zmiany decyzji ADR 0035.
4. A2/E1 — pięć pełnych snapshotów: `a362672`. Wszystkie pola pobranych
   odpowiedzi, adnotacja świadomej naprawy, test obciętych struktur;
   bez zmiany Oracle/kosztu/typu/P/T/flavoru. Pozostałych dwóch
   poprawnych cache'ów nie odświeżano.
5. A3 — złota skala zoomu: `37b2a8f`. Testy montażu i zdarzeń;
   powrót T1→T4→T1 i odwrotnie bez utraty skali. Chromium: Δ 0 px.
6. A4 — mobile: `6a3622d`. Pinezka dostępna, LOD uwzględnia szerokość,
   tytuły mniejsze i mieszczące się przy widocznych kotwicach.
   10/10 hit-testów/kliknięć i otwarć/zamknięć karty w Chromium:
   320/390/600/768/1440 px × T1/T4.
7. A5 — build: `1d2d4fd`. Bez płaskiej kopii SVG przy wariantach;
   HTML Tarkiru 8 482 236 → 4 516 019 bajtów (około −47%, pomiar
   przed checkpointem dokumentacyjnym). Stare mapy i miniatury działają.
8. A6 — L13/gid odróżniają test markupu od zachowania; ROADMAP ma
   aktualny status PR #21, wykonane B2 i pełną erę powierzchni Mirrodinu.
   Historia i „Co nowego” opisują checkpoint bez udawania końca Pętli.

## Kontrole

- A5: **130/130 `npm test`**. W punkcie kontrolnym ponownie
  **130/130 `npm run test:all`**, build zielony, `map-audit` 0.
- 14 stron: 7 kart, 0 haseł, 7 planów; 7 stron map, ZIP 17 plików.
- `map-audit`: 0; determinizm generatorów Mirrodin/Tarkir oraz SVG
  Alara/Mirrodin/Tarkir/Zendikar/demo sprawdzony, zmienione mapy obejrzane.
- Stats **99%, 7,9/8**. Mirrodin 7/8 (brak wychodzących wikilinków),
  pozostałe strony 8/8. To nie ocena prawdziwości ani głębi lore.
- Wszystkie dostawy i oba JPG T1 mają niezmienione SHA-256.
  Pinezka Highland Game nadal `(0.4496,0.1846)`, pewność `region`;
  kalibracja T4 `{sx:0.908609, sy:0.992421, ox:0.045695, oy:0}`.
- Preview: **MTG Lore Codex**, port **8000**, `dist/` na `0.0.0.0`;
  po ostatnim commicie ponowić build dla aktualnej metryki Git.

## Następny krok — Pętla Jakości, nie nowy audyt PR #21

Roadmapa: `docs/plans/PLAN_2026-09-07-audyt-pr21-i-petla-jakosci.md`,
część C. Pass mapowy ma już rzeczywisty rezultat w A1/A3/A4/A5.

1. Ustalić jawny ranking i pogłębić **1–3 strony** z kwerendą lore.
   Robocza diagnostyka wskazała krótkie sekcje „Postacie i Byty” oraz
   „Flavor Text” Aerith Rescue Mission — ocenić ręcznie, nie liczyć
   mechanicznie kropek. Karta NIE ma flavoru: nie wymyślać go i nie
   rozbudowywać sekcji metatekstem. Mirrodin pozostaje kandydatem
   strukturalnym (bez wikilinków), np. pamięć Tel-Jilad/Vanishing.
2. Link-mining: **próg ≥2 KART**, nie dwóch stron kart/planów i nie
   automatyczne „jedna karta na plan = brak wspólnych encji”. Phyrexia
   występuje w Illusory Demon i Carapace Forger. Sprawdzić, czy zakres
   uzasadnia wspólne hasło (frakcja/Nowa Phyrexia, nie mylenie inwazji
   Mirrodinu z późniejszą inwazją Multiwersum). Hasła jeszcze nie ma.
3. Po tej pracy ponownie co-nowego/handoff, testy/build/map-audit,
   push i aktualizacja PR. Nie uznawać korekty kanonu A1 za całą Pętlę.

## Narzędzia i pułapki tej sesji

- Repo nadal zero-dependency. QA poza nim:
  `/home/user/mtg-session/qa/` (resvg, Playwright, Sparticuz Chromium).
  Chromium 149 uruchomiony z `/tmp/chromium`; potrzebne biblioteki
  z `al2023.tar.br`, rozpakowane do `/tmp/al2023/lib`:
  `LD_LIBRARY_PATH=/tmp/al2023/lib`. Zwykłe pobieranie Playwrighta
  z CDN nie działało. `/tmp` może zniknąć — stan trzeba zmierzyć ponownie.
- Obrazy/pomiary/pełne odpowiedzi API: poza repo,
  `/home/user/mtg-session/audit-21/`. Wnioski są w raporcie/roadmapie.
- Poczekać na gotowość aplikacji i obrazu iframe, nie tylko `load`
  rodzica. Przy testowaniu ponownego wejścia pod ten sam URL zamknąć
  dialog lub przejść przez pustą stronę; inaczej poprzednia warstwa
  karty prawidłowo blokuje kliknięcie mapy.
- `gh pr edit` wpada w Projects Classic; działa
  `gh api --method PATCH repos/szybkoiwyraznie-rgb/mtg/pulls/22 -F body=@plik`.
  Używać `gh pr view --json …`, bez domyślnych pól.
- Po odnowieniu tokenu `gh api user` zwróciło 403 dla integracji,
  ale **git fetch/push i API PR działały**. Nie traktować `/user` jako
  obowiązkowego testu autoryzacji. Nie prosić o sekrety.
- `git fetch origin arena/01a07d33-mtg` zapisał `FETCH_HEAD`, bez
  lokalnego remote-tracking refu sesji; porównywać `FETCH_HEAD...HEAD`
  lub sprawdzać zdalny SHA przez `git ls-remote`, nie zakładać refu.
