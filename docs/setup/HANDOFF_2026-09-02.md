# HANDOFF 2026-09-02 — po sesji PR-10 (Pętla Jakości v2: ADR 0021, LORE Zendikaru, E-geo-8/4)

> Handoff = skrót JEDNEJ sesji. Zasady trwałe żyją w AGENTS.md, ADR-ach
> i ENVIRONMENT.md — w razie rozbieżności wygrywa repozytorium.

## Stan projektu

- **PR #10** (`arena/01a0612e-mtg` → `main`) otwarty, do scalenia przez
  właściciela (Squash and merge). Punkt wyjścia = scalony PR #9.
- `npm test` = **87/87**; `npm run build` OK (4 strony, 14 modułów,
  artefakt ~5,8 MB); `tools/map-audit.py` — **0 problemów**.

## Co zrobiono (commity na gałęzi)

1. **Roadmapa sesji** (`docs/plans/PLAN_2026-09-02-pr10-petla-jakosci.md`)
   + PR otwarty PRZED pracą (tryb sesji §1).
2. **Audyt PR #9** (`docs/audits/AUDYT_2026-09-02-PR9.md`): kod i dane
   OK; znalezisko — dryf ADR (decyzje właściciela (a)–(g) z recenzji
   prototypu tylko w komentarzach/dzienniku).
3. **ADR 0021** — formalizacja stylu map T4: jedna barwa wody (zastępuje
   „kolor jeziora" z ADR 0020 pkt 3), kolory funkcjonalne motywu atlas
   (błękit wody + bordowe etykiety — doprecyzowanie ADR 0019), wiążąca
   kolejność warstw, etykiety przy obiektach, szare miasta, pasmo jako
   jedna bryła. Statusy 0019/0020 i tabela rejestru zaktualizowane.
4. **LORE (krok 2):** plan Zendikar/„Ludy" — trzy wiary merfolków,
   trzy narody elfów, trzy plemiona goblinów (+2 źródła: *Plane Shift:
   Zendikar* PDF Wizards, mtglore „Gods and Monsters"); „Geography"→
   „Geografia" na obu planach; literówki.
5. **Pass mapowy (krok 4, tylko T4=Zendikar):**
   - **E-geo-8 rozstrzygnięte:** „Prison of Omath" → kanonicznie
     **„Prison of Omnath"** (MTG Wiki „Ondu"/„Omnath"); scena + map.json
     (notka ze źródłami) + podkład.
   - **E-geo-4 (część):** etykieta **Ior Ruin** przy Glasspool
     (kotwica istniała; kanon: karta *Ior Ruin Expedition* ZEN 49).
   - Regeneracja `podklad.svg` deterministyczna (diff 2 linie), audyt 0.
6. **Krok 3 (link-mining):** bez nowych haseł — żadna encja nie ma
   ≥2 KART (próg właściciela). **Kandydaci na hasła** (gdy dostawy
   podbiją próg): Dunland (1LTR + plan), Halimar/Coralhelm (2BFZ +
   plan), merfolkowie, Saruman/Isengard.

## Stan bazy (po sesji)

- **Strony:** 4 — 2 karty, 2 plany, 0 haseł; completeness 4×100%.
- **Mapy:** Zendikar T4 (mapforge/atlas; ADR 0020+0021), Śródziemie T2
  (mapome, nieruszane — zasada „ubogacanie wyłącznie T3/T4").

## Kolejna sesja

1. Lektura startowa (AGENTS.md §0); najnowszy handoff = ten plik.
2. Bez dostawy → Pętla Jakości: kolejka **E-geo** (ROADMAP): E-geo-1
   (archipelag między Ondu a Akoum — duża operacja), **E-geo-3 (detal
   Murasy: 6 etykiet — WYMAGA wizualnego QA renderu**, nie tylko
   map-audit; procedura rasteryzacji sharp/libvips w handoffie PR-9,
   scratch POZA repo), E-geo-5/6/7/9; **E5 klocki** (cytadela/fort,
   latarnia, wrak, wodospad — kandydat: symbole Azgaar, MIT,
   z atrybucją, ADR 0020 pkt 4).
3. Decyzje właściciela w kolejce (ROADMAP „Wątki otwarte"): skalowanie
   map (osobne pliki SVG vs jednoplik — rewizja ADR 0001/0009),
   E-geo-2 (rotacja układu zachodniego — zmiana globalna).

## Pułapki / obserwacje

- **`scena.json`/`map.json` edytować TEKSTOWO (chirurgicznie)** —
  `json.dumps` przeformatowuje cały plik (inline `[x, y]` → wielolinijkowe)
  i zaśmieca diff; pierwsza próba w tej sesji cofnięta gitem.
- **E-geo-3 nie robić „na ślepo"**: Murasa to najgęstszy region etykiet;
  map-audit łapie kolizje bboxów, ale nie ocenia czytelności — potrzebny
  render + oględziny (sharp/libvips; pełny mapome renderuje się czarno —
  wycinać grupy; handoff PR-9).
- **Styl map T4 = ADR 0021** (jedna barwa wody, kolejność warstw,
  etykiety przy obiektach): nie przywracać `wodaGleb`/gradientów/plam.
- **Wpisy kolekcji nienaruszalne**; zero zmian w `.github/workflows/`
  (brak uprawnień bota, ENVIRONMENT §3).
